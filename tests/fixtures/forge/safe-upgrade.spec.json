{
  "name": "safe-upgrade",
  "description": "Use when planning a dependency upgrade with explicit safety checks and validation.",
  "overview": "Prepare a dependency upgrade, verify its declared scope, and validate the result before completion.",
  "whenToUse": [
    "A project dependency needs a controlled version upgrade.",
    "An upgrade must include validation and rollback guidance."
  ],
  "maturity": "stable",
  "requires": [],
  "routing": {
    "triggers": [
      "safe dependency upgrade",
      "upgrade dependency safely"
    ],
    "antiTriggers": [
      "operating system upgrade"
    ]
  },
  "capabilities": {
    "exec": {
      "allowed": false,
      "commands": []
    },
    "network": {
      "allowed": false,
      "hosts": []
    },
    "write": {
      "scope": "project"
    }
  },
  "compatibility": {
    "claude-code": "full",
    "cursor": "partial"
  },
  "provenance": {
    "source": "tests/fixtures/forge/safe-upgrade.spec.json",
    "license": "MIT"
  }
}
