{
  "schemaVersion": 1,
  "maturity": "experimental",
  "requires": [],
  "routing": {
    "triggers": ["choose support macro", "which faq article", "customer ticket answer"],
    "antiTriggers": ["skillsforge route", "score skill selection", "agent skill package"]
  },
  "capabilities": {
    "exec": { "allowed": false, "commands": [] },
    "network": { "allowed": false, "hosts": [] },
    "write": { "scope": "none" }
  },
  "compatibility": {
    "claude-code": "partial",
    "cursor": "partial"
  }
}
