{
  "schemaVersion": 1,
  "routing": {
    "triggers": ["test safe network"],
    "antiTriggers": []
  },
  "capabilities": {
    "exec": { "allowed": false, "commands": [] },
    "network": { "allowed": true, "hosts": ["api.example.com"] },
    "write": { "scope": "skill" }
  }
}
