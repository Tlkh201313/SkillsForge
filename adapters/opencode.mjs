export function phaseAvailability() {
  return {
    platform: 'opencode',
    activePhase: 4,
    currentPhase: 2,
    support: 'declared-unsupported',
    note: 'OpenCode adapter is deferred; Cursor export proof exists, Claude Code is the real host target.'
  };
}
