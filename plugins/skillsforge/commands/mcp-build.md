---
name: mcp-build
description: Use when invoking SkillsForge mcp-build from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /mcp-build

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `mcp-build` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" route --query "mcp-build $ARGUMENTS"
```
