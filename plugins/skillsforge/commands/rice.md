---
name: rice
description: Use when invoking SkillsForge rice from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /rice

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `rice` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" route --query "rice $ARGUMENTS"
```
