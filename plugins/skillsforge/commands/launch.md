---
name: launch
description: Use when invoking SkillsForge launch from a slash command or host shim.
---

# /launch

Run the SkillsForge `launch` workflow.

```bash
npx skillsforge route --query "launch"
```
