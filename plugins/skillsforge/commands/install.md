---
name: install
description: Use when invoking SkillsForge install from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /install

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `install` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" install $ARGUMENTS
```
