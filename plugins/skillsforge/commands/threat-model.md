---
name: threat-model
description: Use when invoking SkillsForge threat-model from a slash command or host shim.
---

# /threat-model

Run the SkillsForge `threat-model` workflow.

```bash
npx skillsforge route --query "threat-model"
```
