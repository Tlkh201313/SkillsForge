---
name: ceo
description: Use when invoking SkillsForge ceo from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /ceo

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `ceo` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" route --query "ceo $ARGUMENTS"
```
