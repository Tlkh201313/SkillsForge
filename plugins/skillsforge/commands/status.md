---
name: status
description: Use when invoking SkillsForge status from a slash command or host shim.
---

# /status

Run the SkillsForge `status` workflow.

```bash
npx skillsforge route --query "status"
```
