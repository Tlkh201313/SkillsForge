---
name: deploy
description: Use when invoking SkillsForge deploy from a slash command or host shim.
---

# /deploy

Run the SkillsForge `deploy` workflow.

```bash
npx skillsforge route --query "deploy"
```
