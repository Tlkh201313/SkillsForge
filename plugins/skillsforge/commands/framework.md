---
name: framework
description: Use when invoking SkillsForge framework from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /framework

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `framework` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" route --query "framework $ARGUMENTS"
```
