---
name: runbook
description: Use when invoking SkillsForge runbook from a slash command or host shim.
---

# /runbook

Run the SkillsForge `runbook` workflow.

```bash
npx skillsforge route --query "runbook"
```
