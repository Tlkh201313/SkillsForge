---
name: perf
description: Use when invoking SkillsForge perf from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /perf

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `perf` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" route --query "perf $ARGUMENTS"
```
