---
name: media
description: Use when invoking SkillsForge media from a slash command or host shim.
argument-hint: "[args]"
allowed-tools: "Bash(node *),Read"
---

# /media

<!-- generated thin command: prefer upgraded entry commands for trust-critical flows -->

Run the SkillsForge `media` workflow.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" route --query "media $ARGUMENTS"
```
