---
description: Run pressure fixtures for a discipline skill directory
argument-hint: "--skill <dir>"
allowed-tools: "Bash(node *),Read,Write,Edit"
---

# /skillsforge:pressure

Run skill pressure fixtures.

1. Require `--skill <dir>`.
2. Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/skillsforge.mjs" pressure $ARGUMENTS
```

3. Interpret red/green against iron law (fixtures before trusted discipline bodies).
4. Follow skill `pressure-test-skill` for authoring guidance.

