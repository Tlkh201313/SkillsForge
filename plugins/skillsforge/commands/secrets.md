---
name: secrets
description: Use when invoking SkillsForge secrets from a slash command or host shim.
---

# /secrets

Run the SkillsForge `secrets` workflow.

```bash
npx skillsforge route --query "secrets"
```
