{
  "id": "ops.canary-plan",
  "category": "ops",
  "goal": "Plan canary rollout and rollback triggers.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-canary",
      "agent": "release-manager",
      "command": "skillsforge route --query \"Plan canary rollout and rollback triggers.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-feature-flags",
      "agent": "sre"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ops-health",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-canary",
    "eng-feature-flags",
    "ops-health"
  ],
  "recommendedAgents": [
    "release-manager",
    "sre"
  ],
  "commands": [
    "skillsforge auto plan --query \"Plan canary rollout and rollback triggers.\"",
    "skillsforge workflows run --id ops.canary-plan --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
