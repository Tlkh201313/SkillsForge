{
  "id": "ops.capacity-note",
  "category": "ops",
  "goal": "Estimate capacity risk using honest current evidence.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-capacity",
      "agent": "sre",
      "command": "skillsforge route --query \"Estimate capacity risk using honest current evidence.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-load",
      "agent": "perf-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ops-slos",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-capacity",
    "test-load",
    "ops-slos"
  ],
  "recommendedAgents": [
    "sre",
    "perf-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Estimate capacity risk using honest current evidence.\"",
    "skillsforge workflows run --id ops.capacity-note --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
