{
  "id": "ops.slo-draft",
  "category": "ops",
  "goal": "Draft SLOs and failure signals for a workflow.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-slos",
      "agent": "sre",
      "command": "skillsforge route --query \"Draft SLOs and failure signals for a workflow.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "cloud-observability",
      "agent": "pm"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ops-health",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-slos",
    "cloud-observability",
    "ops-health"
  ],
  "recommendedAgents": [
    "sre",
    "pm"
  ],
  "commands": [
    "skillsforge auto plan --query \"Draft SLOs and failure signals for a workflow.\"",
    "skillsforge workflows run --id ops.slo-draft --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
