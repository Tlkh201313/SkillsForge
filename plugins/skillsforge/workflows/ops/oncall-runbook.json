{
  "id": "ops.oncall-runbook",
  "category": "ops",
  "goal": "Prepare on-call runbook for a new capability.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-oncall",
      "agent": "oncall-lead",
      "command": "skillsforge route --query \"Prepare on-call runbook for a new capability.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "ops-runbook-drill",
      "agent": "sre"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ops-runbook-drill",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-oncall",
    "ops-runbook-drill",
    "ops-runbook-drill"
  ],
  "recommendedAgents": [
    "oncall-lead",
    "sre"
  ],
  "commands": [
    "skillsforge auto plan --query \"Prepare on-call runbook for a new capability.\"",
    "skillsforge workflows run --id ops.oncall-runbook --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
