{
  "id": "ops.env-parity",
  "category": "ops",
  "goal": "Compare local, CI, and production configuration assumptions.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-env-parity",
      "agent": "devops-engineer",
      "command": "skillsforge route --query \"Compare local, CI, and production configuration assumptions.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-config-hygiene",
      "agent": "sre"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "cloud-secrets-mgr",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-env-parity",
    "eng-config-hygiene",
    "cloud-secrets-mgr"
  ],
  "recommendedAgents": [
    "devops-engineer",
    "sre"
  ],
  "commands": [
    "skillsforge auto plan --query \"Compare local, CI, and production configuration assumptions.\"",
    "skillsforge workflows run --id ops.env-parity --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
