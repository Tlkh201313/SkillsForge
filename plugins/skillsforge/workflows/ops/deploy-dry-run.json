{
  "id": "ops.deploy-dry-run",
  "category": "ops",
  "goal": "Dry-run deployment commands and rollback checkpoints.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-deploy",
      "agent": "devops-engineer",
      "command": "skillsforge route --query \"Dry-run deployment commands and rollback checkpoints.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-rollback",
      "agent": "release-manager"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ship-release",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-deploy",
    "eng-rollback",
    "ship-release",
    "fullstack-deploy-plan",
    "build-plugin-marketplace-entry",
    "validate-release-readiness"
  ],
  "recommendedAgents": [
    "devops-engineer",
    "release-manager"
  ],
  "commands": [
    "skillsforge auto plan --query \"Dry-run deployment commands and rollback checkpoints.\"",
    "skillsforge workflows run --id ops.deploy-dry-run --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
