{
  "id": "ops.secrets-manager-pass",
  "category": "ops",
  "goal": "Check secret storage and local env docs.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "cloud-secrets-mgr",
      "agent": "security-officer",
      "command": "skillsforge route --query \"Check secret storage and local env docs.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-secrets",
      "agent": "devops-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "docs-runbook",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "cloud-secrets-mgr",
    "sec-secrets",
    "docs-runbook"
  ],
  "recommendedAgents": [
    "security-officer",
    "devops-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Check secret storage and local env docs.\"",
    "skillsforge workflows run --id ops.secrets-manager-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
