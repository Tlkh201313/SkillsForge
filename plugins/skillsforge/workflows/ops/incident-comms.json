{
  "id": "ops.incident-comms",
  "category": "ops",
  "goal": "Write user-facing incident notes without overclaiming.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-incident-comms",
      "agent": "incident-commander",
      "command": "skillsforge route --query \"Write user-facing incident notes without overclaiming.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-incident",
      "agent": "support"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "role-support",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-incident-comms",
    "sec-incident",
    "role-support"
  ],
  "recommendedAgents": [
    "incident-commander",
    "support"
  ],
  "commands": [
    "skillsforge auto plan --query \"Write user-facing incident notes without overclaiming.\"",
    "skillsforge workflows run --id ops.incident-comms --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
