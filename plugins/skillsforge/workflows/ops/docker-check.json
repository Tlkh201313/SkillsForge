{
  "id": "ops.docker-check",
  "category": "ops",
  "goal": "Review Dockerfile and runtime assumptions.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "cloud-docker",
      "agent": "devops-engineer",
      "command": "skillsforge route --query \"Review Dockerfile and runtime assumptions.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "ops-env-parity",
      "agent": "security-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "sec-supply-chain",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "cloud-docker",
    "ops-env-parity",
    "sec-supply-chain"
  ],
  "recommendedAgents": [
    "devops-engineer",
    "security-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Review Dockerfile and runtime assumptions.\"",
    "skillsforge workflows run --id ops.docker-check --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
