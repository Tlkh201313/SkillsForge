{
  "id": "ops.ci-provider-pass",
  "category": "ops",
  "goal": "Verify CI gates match release requirements.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "cloud-ci-providers",
      "agent": "ci-diagnostician",
      "command": "skillsforge route --query \"Verify CI gates match release requirements.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-ci-pipeline",
      "agent": "release-manager"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-smoke",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "cloud-ci-providers",
    "eng-ci-pipeline",
    "test-smoke",
    "ops-ci-failure-router",
    "validate-generated-artifacts",
    "validate-release-readiness"
  ],
  "recommendedAgents": [
    "ci-diagnostician",
    "release-manager"
  ],
  "commands": [
    "skillsforge auto plan --query \"Verify CI gates match release requirements.\"",
    "skillsforge workflows run --id ops.ci-provider-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
