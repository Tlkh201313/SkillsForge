{
  "id": "ops.health-check",
  "category": "ops",
  "goal": "Check service health and required local commands.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "ops-health",
      "agent": "sre",
      "command": "skillsforge route --query \"Check service health and required local commands.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "os-env-doctor",
      "agent": "terminal-output-compressor"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "os-process-port-doctor",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "ops-health",
    "os-env-doctor",
    "os-process-port-doctor"
  ],
  "recommendedAgents": [
    "sre",
    "terminal-output-compressor"
  ],
  "commands": [
    "skillsforge auto plan --query \"Check service health and required local commands.\"",
    "skillsforge workflows run --id ops.health-check --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
