{
  "id": "agentic.safe-tool-policy",
  "category": "agentic",
  "goal": "Define allowed tools before an agent task executes.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "agent-tool-policy",
      "agent": "policy-tester",
      "command": "skillsforge route --query \"Define allowed tools before an agent task executes.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "agent-sandbox",
      "agent": "security-officer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "sec-threat-model",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "agent-tool-policy",
    "agent-sandbox",
    "sec-threat-model"
  ],
  "recommendedAgents": [
    "policy-tester",
    "security-officer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Define allowed tools before an agent task executes.\"",
    "skillsforge workflows run --id agentic.safe-tool-policy --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
