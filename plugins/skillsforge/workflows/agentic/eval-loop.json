{
  "id": "agentic.eval-loop",
  "category": "agentic",
  "goal": "Run an evaluation loop around one agent behavior.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "agent-eval-loop",
      "agent": "eval-engineer",
      "command": "skillsforge route --query \"Run an evaluation loop around one agent behavior.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "pressure-test-skill",
      "agent": "quality-gate"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "research-evidence-grade",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "agent-eval-loop",
    "pressure-test-skill",
    "research-evidence-grade"
  ],
  "recommendedAgents": [
    "eval-engineer",
    "quality-gate"
  ],
  "commands": [
    "skillsforge auto plan --query \"Run an evaluation loop around one agent behavior.\"",
    "skillsforge workflows run --id agentic.eval-loop --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
