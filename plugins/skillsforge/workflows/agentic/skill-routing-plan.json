{
  "id": "agentic.skill-routing-plan",
  "category": "agentic",
  "goal": "Pick one skill or workflow from the local library.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "route-capability",
      "agent": "workflow-curator",
      "command": "skillsforge route --query \"Pick one skill or workflow from the local library.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "browse-catalog",
      "agent": "llm-index-librarian"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "using-skillsforge",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "route-capability",
    "browse-catalog",
    "using-skillsforge",
    "build-agent-skill",
    "build-skill-library-index",
    "agentic-orchestrator-plan",
    "agentic-workflow-replay"
  ],
  "recommendedAgents": [
    "workflow-curator",
    "llm-index-librarian"
  ],
  "commands": [
    "skillsforge auto plan --query \"Pick one skill or workflow from the local library.\"",
    "skillsforge workflows run --id agentic.skill-routing-plan --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
