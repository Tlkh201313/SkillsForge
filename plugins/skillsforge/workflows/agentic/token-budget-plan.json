{
  "id": "agentic.token-budget-plan",
  "category": "agentic",
  "goal": "Plan agent work to minimize unnecessary file reads.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "agent-prompt-budget",
      "agent": "token-budget-controller",
      "command": "skillsforge route --query \"Plan agent work to minimize unnecessary file reads.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "agent-context",
      "agent": "planner"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "route-capability",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "agent-prompt-budget",
    "agent-context",
    "route-capability"
  ],
  "recommendedAgents": [
    "token-budget-controller",
    "planner"
  ],
  "commands": [
    "skillsforge auto plan --query \"Plan agent work to minimize unnecessary file reads.\"",
    "skillsforge workflows run --id agentic.token-budget-plan --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
