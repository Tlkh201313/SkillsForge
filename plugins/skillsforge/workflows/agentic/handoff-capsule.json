{
  "id": "agentic.handoff-capsule",
  "category": "agentic",
  "goal": "Prepare a compact handoff with exact files and gates.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "agent-handoff",
      "agent": "handoff-compressor",
      "command": "skillsforge route --query \"Prepare a compact handoff with exact files and gates.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "agent-context",
      "agent": "tech-writer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "docs-llms-txt",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "agent-handoff",
    "agent-context",
    "docs-llms-txt"
  ],
  "recommendedAgents": [
    "handoff-compressor",
    "tech-writer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Prepare a compact handoff with exact files and gates.\"",
    "skillsforge workflows run --id agentic.handoff-capsule --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
