{
  "id": "media.asset-compression",
  "category": "media",
  "goal": "Compress assets while preserving readable demo quality.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "media-compression",
      "agent": "media-producer",
      "command": "skillsforge route --query \"Compress assets while preserving readable demo quality.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "ops-capacity",
      "agent": "perf-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-visual",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "media-compression",
    "media-remotion-render-proof",
    "media-video-quality-gate",
    "ops-capacity",
    "test-visual"
  ],
  "recommendedAgents": [
    "media-producer",
    "perf-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Compress assets while preserving readable demo quality.\"",
    "skillsforge workflows run --id media.asset-compression --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
