{
  "id": "media.frame-sampling",
  "category": "media",
  "goal": "Sample video frames to verify the product is visible.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "media-frame-sampling",
      "agent": "video-workflow-producer",
      "command": "skillsforge route --query \"Sample video frames to verify the product is visible.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "media-video-watch",
      "agent": "motion-qa"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-visual",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "media-frame-sampling",
    "media-video-watch",
    "test-visual"
  ],
  "recommendedAgents": [
    "video-workflow-producer",
    "motion-qa"
  ],
  "commands": [
    "skillsforge auto plan --query \"Sample video frames to verify the product is visible.\"",
    "skillsforge workflows run --id media.frame-sampling --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
