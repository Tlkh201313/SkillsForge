{
  "id": "docs.api-reference",
  "category": "docs",
  "goal": "Document CLI/API inputs, outputs, and exit codes.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "docs-api-ref",
      "agent": "api-designer",
      "command": "skillsforge route --query \"Document CLI/API inputs, outputs, and exit codes.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-api-design",
      "agent": "tech-writer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-contract",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "docs-api-ref",
    "eng-api-design",
    "test-contract"
  ],
  "recommendedAgents": [
    "api-designer",
    "tech-writer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Document CLI/API inputs, outputs, and exit codes.\"",
    "skillsforge workflows run --id docs.api-reference --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
