{
  "id": "docs.quickstart-test",
  "category": "docs",
  "goal": "Make quickstart steps executable from a clean clone.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "docs-quickstart",
      "agent": "docs-engineer",
      "command": "skillsforge route --query \"Make quickstart steps executable from a clean clone.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "run-build",
      "agent": "devex-reviewer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-smoke",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "docs-quickstart",
    "run-build",
    "test-smoke"
  ],
  "recommendedAgents": [
    "docs-engineer",
    "devex-reviewer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Make quickstart steps executable from a clean clone.\"",
    "skillsforge workflows run --id docs.quickstart-test --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
