{
  "id": "docs.contributing-guide",
  "category": "docs",
  "goal": "Clarify contribution checks and local setup.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "docs-contributing",
      "agent": "devex-reviewer",
      "command": "skillsforge route --query \"Clarify contribution checks and local setup.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "docs-quickstart",
      "agent": "docs-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "run-build",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "docs-contributing",
    "docs-quickstart",
    "run-build"
  ],
  "recommendedAgents": [
    "devex-reviewer",
    "docs-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Clarify contribution checks and local setup.\"",
    "skillsforge workflows run --id docs.contributing-guide --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
