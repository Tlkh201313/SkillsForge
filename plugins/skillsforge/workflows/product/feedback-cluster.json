{
  "id": "product.feedback-cluster",
  "category": "product",
  "goal": "Cluster user feedback into product decisions.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "product-feedback",
      "agent": "research-synthesizer",
      "command": "skillsforge route --query \"Cluster user feedback into product decisions.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "research-synth",
      "agent": "pm"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "research-contradictions",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "product-feedback",
    "research-synth",
    "research-contradictions"
  ],
  "recommendedAgents": [
    "research-synthesizer",
    "pm"
  ],
  "commands": [
    "skillsforge auto plan --query \"Cluster user feedback into product decisions.\"",
    "skillsforge workflows run --id product.feedback-cluster --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
