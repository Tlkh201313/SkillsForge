{
  "id": "security.secret-scan",
  "category": "security",
  "goal": "Check secrets handling before packaging or release.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-secrets",
      "agent": "security-officer",
      "command": "skillsforge route --query \"Check secrets handling before packaging or release.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-secrets-handling",
      "agent": "package-auditor"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "sec-secrets",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-secrets",
    "eng-secrets-handling",
    "sec-secrets"
  ],
  "recommendedAgents": [
    "security-officer",
    "package-auditor"
  ],
  "commands": [
    "skillsforge auto plan --query \"Check secrets handling before packaging or release.\"",
    "skillsforge workflows run --id security.secret-scan --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
