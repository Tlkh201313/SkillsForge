{
  "id": "security.owasp-pass",
  "category": "security",
  "goal": "Run an OWASP-oriented checklist for a web or API change.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-owasp",
      "agent": "security-engineer",
      "command": "skillsforge route --query \"Run an OWASP-oriented checklist for a web or API change.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-input-validation",
      "agent": "qa-lead"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "sec-authz",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-owasp",
    "sec-input-validation",
    "sec-authz"
  ],
  "recommendedAgents": [
    "security-engineer",
    "qa-lead"
  ],
  "commands": [
    "skillsforge auto plan --query \"Run an OWASP-oriented checklist for a web or API change.\"",
    "skillsforge workflows run --id security.owasp-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
