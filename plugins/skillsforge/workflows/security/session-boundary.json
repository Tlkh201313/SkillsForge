{
  "id": "security.session-boundary",
  "category": "security",
  "goal": "Check session and credential boundaries.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-session",
      "agent": "privacy-officer",
      "command": "skillsforge route --query \"Check session and credential boundaries.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-privacy",
      "agent": "security-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-security",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-session",
    "sec-privacy",
    "test-security"
  ],
  "recommendedAgents": [
    "privacy-officer",
    "security-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Check session and credential boundaries.\"",
    "skillsforge workflows run --id security.session-boundary --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
