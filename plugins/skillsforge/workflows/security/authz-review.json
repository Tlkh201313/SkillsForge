{
  "id": "security.authz-review",
  "category": "security",
  "goal": "Review authorization checks around a sensitive action.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-authz",
      "agent": "security-engineer",
      "command": "skillsforge route --query \"Review authorization checks around a sensitive action.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-security",
      "agent": "backend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "eng-error-handling",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-authz",
    "test-security",
    "eng-error-handling"
  ],
  "recommendedAgents": [
    "security-engineer",
    "backend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Review authorization checks around a sensitive action.\"",
    "skillsforge workflows run --id security.authz-review --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
