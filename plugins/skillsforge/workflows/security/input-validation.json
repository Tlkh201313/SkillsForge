{
  "id": "security.input-validation",
  "category": "security",
  "goal": "Harden parsers, CLI flags, and file path inputs.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-input-validation",
      "agent": "policy-tester",
      "command": "skillsforge route --query \"Harden parsers, CLI flags, and file path inputs.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-api-design",
      "agent": "backend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-security",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-input-validation",
    "eng-api-design",
    "test-security"
  ],
  "recommendedAgents": [
    "policy-tester",
    "backend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Harden parsers, CLI flags, and file path inputs.\"",
    "skillsforge workflows run --id security.input-validation --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
