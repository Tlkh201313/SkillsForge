{
  "id": "security.supply-chain-package",
  "category": "security",
  "goal": "Review package contents and provenance before publish.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-supply-chain",
      "agent": "package-auditor",
      "command": "skillsforge route --query \"Review package contents and provenance before publish.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "legal-oss-notice",
      "agent": "release-manager"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ship-release",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-supply-chain",
    "legal-oss-notice",
    "ship-release"
  ],
  "recommendedAgents": [
    "package-auditor",
    "release-manager"
  ],
  "commands": [
    "skillsforge auto plan --query \"Review package contents and provenance before publish.\"",
    "skillsforge workflows run --id security.supply-chain-package --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
