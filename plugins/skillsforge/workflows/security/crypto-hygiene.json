{
  "id": "security.crypto-hygiene",
  "category": "security",
  "goal": "Review crypto choices for misuse and unsafe defaults.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "sec-crypto-hygiene",
      "agent": "security-engineer",
      "command": "skillsforge route --query \"Review crypto choices for misuse and unsafe defaults.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-threat-model",
      "agent": "policy-tester"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-security",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "sec-crypto-hygiene",
    "sec-threat-model",
    "test-security"
  ],
  "recommendedAgents": [
    "security-engineer",
    "policy-tester"
  ],
  "commands": [
    "skillsforge auto plan --query \"Review crypto choices for misuse and unsafe defaults.\"",
    "skillsforge workflows run --id security.crypto-hygiene --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
