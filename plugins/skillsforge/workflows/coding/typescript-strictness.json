{
  "id": "coding.typescript-strictness",
  "category": "coding",
  "goal": "Tighten types around one feature without changing runtime behavior.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-types-strict",
      "agent": "frontend-dev",
      "command": "skillsforge route --query \"Tighten types around one feature without changing runtime behavior.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-unit",
      "agent": "backend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "review-diff",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-types-strict",
    "test-unit",
    "review-diff"
  ],
  "recommendedAgents": [
    "frontend-dev",
    "backend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Tighten types around one feature without changing runtime behavior.\"",
    "skillsforge workflows run --id coding.typescript-strictness --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
