{
  "id": "coding.dependency-upgrade",
  "category": "coding",
  "goal": "Upgrade dependencies with lockfile, audit, and rollback checks.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-deps-upgrade",
      "agent": "dependency-upgrade-surgeon",
      "command": "skillsforge route --query \"Upgrade dependencies with lockfile, audit, and rollback checks.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-deps",
      "agent": "package-auditor"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "run-build",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-deps-upgrade",
    "sec-deps",
    "run-build"
  ],
  "recommendedAgents": [
    "dependency-upgrade-surgeon",
    "package-auditor"
  ],
  "commands": [
    "skillsforge auto plan --query \"Upgrade dependencies with lockfile, audit, and rollback checks.\"",
    "skillsforge workflows run --id coding.dependency-upgrade --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
