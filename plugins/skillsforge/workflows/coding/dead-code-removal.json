{
  "id": "coding.dead-code-removal",
  "category": "coding",
  "goal": "Remove dead code only after reachability and tests prove it unused.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-dead-code",
      "agent": "eng-reviewer",
      "command": "skillsforge route --query \"Remove dead code only after reachability and tests prove it unused.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "review-diff",
      "agent": "quality-gate"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "run-build",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-dead-code",
    "review-diff",
    "run-build"
  ],
  "recommendedAgents": [
    "eng-reviewer",
    "quality-gate"
  ],
  "commands": [
    "skillsforge auto plan --query \"Remove dead code only after reachability and tests prove it unused.\"",
    "skillsforge workflows run --id coding.dead-code-removal --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
