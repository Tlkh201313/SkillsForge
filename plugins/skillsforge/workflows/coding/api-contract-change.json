{
  "id": "coding.api-contract-change",
  "category": "coding",
  "goal": "Change an API contract while preserving compatibility notes.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-api-design",
      "agent": "api-designer",
      "command": "skillsforge route --query \"Change an API contract while preserving compatibility notes.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-contracts",
      "agent": "backend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-contract",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-api-design",
    "eng-contracts",
    "test-contract",
    "build-mcp-server",
    "build-mcp-tool-contract",
    "validate-mcp-contract",
    "build-plugin-marketplace-entry"
  ],
  "recommendedAgents": [
    "api-designer",
    "backend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Change an API contract while preserving compatibility notes.\"",
    "skillsforge workflows run --id coding.api-contract-change --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
