{
  "id": "coding.release-diff-review",
  "category": "coding",
  "goal": "Review a release diff for risk, generated files, and docs drift.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "review-diff",
      "agent": "release-manager",
      "command": "skillsforge route --query \"Review a release diff for risk, generated files, and docs drift.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "ship-release",
      "agent": "quality-gate"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "prove-outcome",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "review-diff",
    "ship-release",
    "prove-outcome"
  ],
  "recommendedAgents": [
    "release-manager",
    "quality-gate"
  ],
  "commands": [
    "skillsforge auto plan --query \"Review a release diff for risk, generated files, and docs drift.\"",
    "skillsforge workflows run --id coding.release-diff-review --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
