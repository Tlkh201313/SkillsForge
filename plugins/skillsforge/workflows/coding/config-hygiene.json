{
  "id": "coding.config-hygiene",
  "category": "coding",
  "goal": "Normalize project configuration and document source of truth.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-config-hygiene",
      "agent": "devex-reviewer",
      "command": "skillsforge route --query \"Normalize project configuration and document source of truth.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "docs-runbook",
      "agent": "docs-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "run-build",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-config-hygiene",
    "docs-runbook",
    "run-build"
  ],
  "recommendedAgents": [
    "devex-reviewer",
    "docs-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Normalize project configuration and document source of truth.\"",
    "skillsforge workflows run --id coding.config-hygiene --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
