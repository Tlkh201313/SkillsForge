{
  "id": "coding.migration-safety",
  "category": "coding",
  "goal": "Plan and validate a migration with rollback and fixtures.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-migrations",
      "agent": "data-engineer",
      "command": "skillsforge route --query \"Plan and validate a migration with rollback and fixtures.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "data-backfill",
      "agent": "backend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-contract",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-migrations",
    "data-backfill",
    "test-contract"
  ],
  "recommendedAgents": [
    "data-engineer",
    "backend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Plan and validate a migration with rollback and fixtures.\"",
    "skillsforge workflows run --id coding.migration-safety --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
