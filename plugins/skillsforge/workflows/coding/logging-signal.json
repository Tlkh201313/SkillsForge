{
  "id": "coding.logging-signal",
  "category": "coding",
  "goal": "Improve logs so failures are diagnosable without noisy output.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-logging",
      "agent": "sre",
      "command": "skillsforge route --query \"Improve logs so failures are diagnosable without noisy output.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-observability",
      "agent": "backend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "review-diff",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-logging",
    "eng-observability",
    "review-diff"
  ],
  "recommendedAgents": [
    "sre",
    "backend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Improve logs so failures are diagnosable without noisy output.\"",
    "skillsforge workflows run --id coding.logging-signal --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
