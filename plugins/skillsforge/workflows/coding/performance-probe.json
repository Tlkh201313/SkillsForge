{
  "id": "coding.performance-probe",
  "category": "coding",
  "goal": "Profile a slow path before optimizing code.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-perf-profile",
      "agent": "perf-engineer",
      "command": "skillsforge route --query \"Profile a slow path before optimizing code.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "spike-explore",
      "agent": "bench-runner"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-load",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-perf-profile",
    "spike-explore",
    "test-load"
  ],
  "recommendedAgents": [
    "perf-engineer",
    "bench-runner"
  ],
  "commands": [
    "skillsforge auto plan --query \"Profile a slow path before optimizing code.\"",
    "skillsforge workflows run --id coding.performance-probe --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
