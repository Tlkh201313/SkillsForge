{
  "id": "coding.error-handling-path",
  "category": "coding",
  "goal": "Add explicit error handling and tests around failure states.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "eng-error-handling",
      "agent": "debugger",
      "command": "skillsforge route --query \"Add explicit error handling and tests around failure states.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-regression",
      "agent": "qa-lead"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "debug-issue",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "eng-error-handling",
    "test-regression",
    "debug-issue"
  ],
  "recommendedAgents": [
    "debugger",
    "qa-lead"
  ],
  "commands": [
    "skillsforge auto plan --query \"Add explicit error handling and tests around failure states.\"",
    "skillsforge workflows run --id coding.error-handling-path --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
