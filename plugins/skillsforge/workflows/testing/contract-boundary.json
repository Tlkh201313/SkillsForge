{
  "id": "testing.contract-boundary",
  "category": "testing",
  "goal": "Validate inputs and outputs at a service boundary.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-contract",
      "agent": "api-designer",
      "command": "skillsforge route --query \"Validate inputs and outputs at a service boundary.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-contracts",
      "agent": "qa-lead"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "eng-api-design",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-contract",
    "eng-contracts",
    "eng-api-design"
  ],
  "recommendedAgents": [
    "api-designer",
    "qa-lead"
  ],
  "commands": [
    "skillsforge auto plan --query \"Validate inputs and outputs at a service boundary.\"",
    "skillsforge workflows run --id testing.contract-boundary --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
