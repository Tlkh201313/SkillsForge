{
  "id": "testing.unit-test-gap",
  "category": "testing",
  "goal": "Add focused unit tests for one changed behavior.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-unit",
      "agent": "test-automator",
      "command": "skillsforge route --query \"Add focused unit tests for one changed behavior.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "tdd-first",
      "agent": "builder"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "run-build",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-unit",
    "tdd-first",
    "run-build"
  ],
  "recommendedAgents": [
    "test-automator",
    "builder"
  ],
  "commands": [
    "skillsforge auto plan --query \"Add focused unit tests for one changed behavior.\"",
    "skillsforge workflows run --id testing.unit-test-gap --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
