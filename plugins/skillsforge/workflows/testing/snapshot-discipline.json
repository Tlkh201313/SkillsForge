{
  "id": "testing.snapshot-discipline",
  "category": "testing",
  "goal": "Use snapshots only for stable generated output.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-snapshot",
      "agent": "test-automator",
      "command": "skillsforge route --query \"Use snapshots only for stable generated output.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "review-diff",
      "agent": "quality-gate"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "docs-api-ref",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-snapshot",
    "review-diff",
    "docs-api-ref"
  ],
  "recommendedAgents": [
    "test-automator",
    "quality-gate"
  ],
  "commands": [
    "skillsforge auto plan --query \"Use snapshots only for stable generated output.\"",
    "skillsforge workflows run --id testing.snapshot-discipline --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
