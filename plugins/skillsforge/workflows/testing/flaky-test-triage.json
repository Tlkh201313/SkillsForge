{
  "id": "testing.flaky-test-triage",
  "category": "testing",
  "goal": "Triage a flaky test without masking real failures.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-flaky",
      "agent": "qa-lead",
      "command": "skillsforge route --query \"Triage a flaky test without masking real failures.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "debug-issue",
      "agent": "debugger"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ops-health",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-flaky",
    "debug-issue",
    "ops-health"
  ],
  "recommendedAgents": [
    "qa-lead",
    "debugger"
  ],
  "commands": [
    "skillsforge auto plan --query \"Triage a flaky test without masking real failures.\"",
    "skillsforge workflows run --id testing.flaky-test-triage --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
