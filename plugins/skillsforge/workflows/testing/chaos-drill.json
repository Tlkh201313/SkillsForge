{
  "id": "testing.chaos-drill",
  "category": "testing",
  "goal": "Dry-run failure behavior for retries, timeouts, and rollback.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-chaos",
      "agent": "sre",
      "command": "skillsforge route --query \"Dry-run failure behavior for retries, timeouts, and rollback.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "ops-runbook-drill",
      "agent": "incident-commander"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "eng-rollback",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-chaos",
    "ops-runbook-drill",
    "eng-rollback"
  ],
  "recommendedAgents": [
    "sre",
    "incident-commander"
  ],
  "commands": [
    "skillsforge auto plan --query \"Dry-run failure behavior for retries, timeouts, and rollback.\"",
    "skillsforge workflows run --id testing.chaos-drill --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
