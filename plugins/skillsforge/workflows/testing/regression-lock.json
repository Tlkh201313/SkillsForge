{
  "id": "testing.regression-lock",
  "category": "testing",
  "goal": "Convert a reproduced bug into a regression test.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-regression",
      "agent": "debugger",
      "command": "skillsforge route --query \"Convert a reproduced bug into a regression test.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "debug-issue",
      "agent": "test-automator"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "tdd-first",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-regression",
    "debug-issue",
    "tdd-first",
    "validate-real-task",
    "validate-feature-claim",
    "validate-workflow-runbook"
  ],
  "recommendedAgents": [
    "debugger",
    "test-automator"
  ],
  "commands": [
    "skillsforge auto plan --query \"Convert a reproduced bug into a regression test.\"",
    "skillsforge workflows run --id testing.regression-lock --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
