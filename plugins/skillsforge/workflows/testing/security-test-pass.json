{
  "id": "testing.security-test-pass",
  "category": "testing",
  "goal": "Add tests around auth, input validation, or unsafe command paths.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-security",
      "agent": "security-engineer",
      "command": "skillsforge route --query \"Add tests around auth, input validation, or unsafe command paths.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "sec-input-validation",
      "agent": "test-automator"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "sec-authz",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-security",
    "sec-input-validation",
    "sec-authz"
  ],
  "recommendedAgents": [
    "security-engineer",
    "test-automator"
  ],
  "commands": [
    "skillsforge auto plan --query \"Add tests around auth, input validation, or unsafe command paths.\"",
    "skillsforge workflows run --id testing.security-test-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "high"
}
