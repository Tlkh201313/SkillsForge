{
  "id": "testing.load-test-probe",
  "category": "testing",
  "goal": "Run a small load probe and document limits honestly.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "test-load",
      "agent": "perf-engineer",
      "command": "skillsforge route --query \"Run a small load probe and document limits honestly.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "eng-perf-profile",
      "agent": "sre"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "ops-capacity",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "test-load",
    "eng-perf-profile",
    "ops-capacity"
  ],
  "recommendedAgents": [
    "perf-engineer",
    "sre"
  ],
  "commands": [
    "skillsforge auto plan --query \"Run a small load probe and document limits honestly.\"",
    "skillsforge workflows run --id testing.load-test-probe --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
