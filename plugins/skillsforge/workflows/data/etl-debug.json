{
  "id": "data.etl-debug",
  "category": "data",
  "goal": "Debug ETL failures with sample records and replay.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "data-etl",
      "agent": "data-engineer",
      "command": "skillsforge route --query \"Debug ETL failures with sample records and replay.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "debug-issue",
      "agent": "debugger"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "data-quality",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "data-etl",
    "debug-issue",
    "data-quality"
  ],
  "recommendedAgents": [
    "data-engineer",
    "debugger"
  ],
  "commands": [
    "skillsforge auto plan --query \"Debug ETL failures with sample records and replay.\"",
    "skillsforge workflows run --id data.etl-debug --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
