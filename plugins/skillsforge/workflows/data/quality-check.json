{
  "id": "data.quality-check",
  "category": "data",
  "goal": "Add checks for missing, duplicate, and invalid records.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "data-quality",
      "agent": "data-quality-auditor",
      "command": "skillsforge route --query \"Add checks for missing, duplicate, and invalid records.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-contract",
      "agent": "data-engineer"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "data-lineage",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "data-quality",
    "test-contract",
    "data-lineage"
  ],
  "recommendedAgents": [
    "data-quality-auditor",
    "data-engineer"
  ],
  "commands": [
    "skillsforge auto plan --query \"Add checks for missing, duplicate, and invalid records.\"",
    "skillsforge workflows run --id data.quality-check --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
