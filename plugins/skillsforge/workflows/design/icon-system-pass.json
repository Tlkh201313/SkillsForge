{
  "id": "design.icon-system-pass",
  "category": "design",
  "goal": "Ensure icons follow one family and one action model.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "design-icons",
      "agent": "design-reviewer",
      "command": "skillsforge route --query \"Ensure icons follow one family and one action model.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "design-system",
      "agent": "frontend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "design-a11y",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "design-icons",
    "design-system",
    "design-a11y"
  ],
  "recommendedAgents": [
    "design-reviewer",
    "frontend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Ensure icons follow one family and one action model.\"",
    "skillsforge workflows run --id design.icon-system-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
