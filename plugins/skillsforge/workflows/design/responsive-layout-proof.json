{
  "id": "design.responsive-layout-proof",
  "category": "design",
  "goal": "Verify responsive UI layout without text overlap.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "design-responsive",
      "agent": "ui-systems-designer",
      "command": "skillsforge route --query \"Verify responsive UI layout without text overlap.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-visual",
      "agent": "motion-qa"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "design-a11y",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "design-responsive",
    "test-visual",
    "design-a11y"
  ],
  "recommendedAgents": [
    "ui-systems-designer",
    "motion-qa"
  ],
  "commands": [
    "skillsforge auto plan --query \"Verify responsive UI layout without text overlap.\"",
    "skillsforge workflows run --id design.responsive-layout-proof --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
