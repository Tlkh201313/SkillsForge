{
  "id": "design.dark-mode-pass",
  "category": "design",
  "goal": "Verify one theme strategy across light and dark modes.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "design-dark-mode",
      "agent": "ui-systems-designer",
      "command": "skillsforge route --query \"Verify one theme strategy across light and dark modes.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "design-color",
      "agent": "qa-lead"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "test-visual",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "design-dark-mode",
    "design-color",
    "test-visual"
  ],
  "recommendedAgents": [
    "ui-systems-designer",
    "qa-lead"
  ],
  "commands": [
    "skillsforge auto plan --query \"Verify one theme strategy across light and dark modes.\"",
    "skillsforge workflows run --id design.dark-mode-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
