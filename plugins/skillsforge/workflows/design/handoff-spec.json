{
  "id": "design.handoff-spec",
  "category": "design",
  "goal": "Write implementation-ready design notes for a UI change.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "design-handoff",
      "agent": "designer",
      "command": "skillsforge route --query \"Write implementation-ready design notes for a UI change.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "docs-api-ref",
      "agent": "frontend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "plan-work",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "design-handoff",
    "docs-api-ref",
    "plan-work"
  ],
  "recommendedAgents": [
    "designer",
    "frontend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Write implementation-ready design notes for a UI change.\"",
    "skillsforge workflows run --id design.handoff-spec --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
