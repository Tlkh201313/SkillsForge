{
  "id": "design.motion-microstates",
  "category": "design",
  "goal": "Add restrained motion only for state feedback.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "design-motion",
      "agent": "motion-qa",
      "command": "skillsforge route --query \"Add restrained motion only for state feedback.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "test-visual",
      "agent": "frontend-dev"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "design-a11y",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "design-motion",
    "test-visual",
    "design-a11y"
  ],
  "recommendedAgents": [
    "motion-qa",
    "frontend-dev"
  ],
  "commands": [
    "skillsforge auto plan --query \"Add restrained motion only for state feedback.\"",
    "skillsforge workflows run --id design.motion-microstates --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "low"
}
