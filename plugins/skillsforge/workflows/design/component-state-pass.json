{
  "id": "design.component-state-pass",
  "category": "design",
  "goal": "Check empty, loading, error, active, and focus states.",
  "inputs": [
    "task brief",
    "repo path",
    "verification command",
    "risk constraints"
  ],
  "steps": [
    {
      "id": "shape",
      "action": "State goal, risk, and smallest useful scope.",
      "skill": "shape-intent",
      "agent": "planner"
    },
    {
      "id": "route",
      "action": "Route to the smallest matching SkillsForge skill set.",
      "skill": "design-system",
      "agent": "frontend-dev",
      "command": "skillsforge route --query \"Check empty, loading, error, active, and focus states.\" --include-explicit"
    },
    {
      "id": "execute-plan",
      "action": "Perform only the bounded task steps and keep output compact.",
      "skill": "design-forms",
      "agent": "qa-lead"
    },
    {
      "id": "verify",
      "action": "Run the listed quality gate and record evidence.",
      "skill": "qa-flow",
      "agent": "quality-gate",
      "command": "skillsforge wb proof"
    }
  ],
  "recommendedSkills": [
    "design-system",
    "design-forms",
    "qa-flow"
  ],
  "recommendedAgents": [
    "frontend-dev",
    "qa-lead"
  ],
  "commands": [
    "skillsforge auto plan --query \"Check empty, loading, error, active, and focus states.\"",
    "skillsforge workflows run --id design.component-state-pass --dry-run",
    "skillsforge wb status --json"
  ],
  "stopGates": [
    "scope is explicit",
    "no unrelated files changed",
    "quality gate passes",
    "final diff is reviewed"
  ],
  "tokenBudget": {
    "mode": "compact",
    "target": "read only routed files and command summaries"
  },
  "qualityGate": "Run the smallest relevant test or proof command and keep the result attached to the final note.",
  "risk": "medium"
}
