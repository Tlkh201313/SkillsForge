interface:
  display_name: Startup Idea Filter
  short_description: Use when doing startup idea filter work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and pr
  default_prompt: Use the startup-idea-filter skill for this task.
policy:
  allow_implicit_invocation: false
