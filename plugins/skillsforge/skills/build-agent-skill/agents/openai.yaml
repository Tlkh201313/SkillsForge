interface:
  display_name: Build Agent Skill
  short_description: Use when doing build agent skill work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation path bef
  default_prompt: Use the build-agent-skill skill for this task.
policy:
  allow_implicit_invocation: false
