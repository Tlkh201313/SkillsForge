interface:
  display_name: Growth Launch
  short_description: Use when doing growth launch work and you need bounded growth-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the growth-launch skill for this task.
policy:
  allow_implicit_invocation: false
