interface:
  display_name: Growth Launch
  short_description: Use when doing growth launch work for launch, SEO, referral, email, proof, and growth loop work and you need audience, channel, offer, experiment, metric, creative asset, and verification step before
  default_prompt: Use the growth-launch skill for this task.
policy:
  allow_implicit_invocation: false
