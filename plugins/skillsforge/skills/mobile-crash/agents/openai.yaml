interface:
  display_name: Mobile Crash
  short_description: Use when doing mobile crash work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-crash skill for this task.
policy:
  allow_implicit_invocation: false
