interface:
  display_name: Agent Fanout
  short_description: Use when doing agent fanout work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay path, and ha
  default_prompt: Use the agent-fanout skill for this task.
policy:
  allow_implicit_invocation: false
