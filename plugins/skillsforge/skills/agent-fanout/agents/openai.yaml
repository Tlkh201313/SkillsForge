interface:
  display_name: Agent Fanout
  short_description: Use when doing agent fanout work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-fanout skill for this task.
policy:
  allow_implicit_invocation: false
