interface:
  display_name: Triage Inbox
  short_description: Use when doing triage inbox work for lifecycle-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the triage-inbox skill for this task.
policy:
  allow_implicit_invocation: false
