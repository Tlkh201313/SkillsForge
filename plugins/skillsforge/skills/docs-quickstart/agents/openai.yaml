interface:
  display_name: Docs Quickstart
  short_description: Use when doing docs quickstart work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-quickstart skill for this task.
policy:
  allow_implicit_invocation: false
