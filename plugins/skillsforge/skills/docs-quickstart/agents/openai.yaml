interface:
  display_name: Docs Quickstart
  short_description: Use when doing docs quickstart work for agent-facing docs, launch media, demo proof, and content production and you need audience, source facts, outline, asset path, claim proof, and publishing check
  default_prompt: Use the docs-quickstart skill for this task.
policy:
  allow_implicit_invocation: false
