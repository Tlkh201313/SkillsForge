interface:
  display_name: Design Before Code
  short_description: Use when doing design before code work and you need bounded methodology-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-before-code skill for this task.
policy:
  allow_implicit_invocation: false
