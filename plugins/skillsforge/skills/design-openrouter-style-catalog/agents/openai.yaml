interface:
  display_name: Design Openrouter Style Catalog
  short_description: Use when doing design openrouter style catalog work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, acc
  default_prompt: Use the design-openrouter-style-catalog skill for this task.
policy:
  allow_implicit_invocation: false
