interface:
  display_name: Fw Docker
  short_description: Use when doing fw docker work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-docker skill for this task.
policy:
  allow_implicit_invocation: false
