interface:
  display_name: Fw Docker
  short_description: Use when you need fw docker in a SkillsForge framework workflow.
  default_prompt: Use the fw-docker skill for this task.
policy:
  allow_implicit_invocation: true
