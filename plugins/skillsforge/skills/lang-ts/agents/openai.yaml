interface:
  display_name: Lang Ts
  short_description: Use when doing lang ts work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-ts skill for this task.
policy:
  allow_implicit_invocation: false
