interface:
  display_name: Lang Ts
  short_description: Use when doing lang ts work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-ts skill for this task.
policy:
  allow_implicit_invocation: false
