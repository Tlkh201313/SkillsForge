interface:
  display_name: Content Cta
  short_description: Use when doing content cta work and you need bounded content-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the content-cta skill for this task.
policy:
  allow_implicit_invocation: false
