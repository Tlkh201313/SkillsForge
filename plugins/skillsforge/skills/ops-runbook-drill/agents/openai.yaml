interface:
  display_name: Ops Runbook Drill
  short_description: Use when you need ops runbook drill in a SkillsForge ops workflow.
  default_prompt: Use the ops-runbook-drill skill for this task.
policy:
  allow_implicit_invocation: true
