interface:
  display_name: Ops Runbook Drill
  short_description: Use when doing ops runbook drill work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runb
  default_prompt: Use the ops-runbook-drill skill for this task.
policy:
  allow_implicit_invocation: false
