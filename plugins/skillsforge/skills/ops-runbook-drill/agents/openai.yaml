interface:
  display_name: Ops Runbook Drill
  short_description: Use when doing ops runbook drill work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-runbook-drill skill for this task.
policy:
  allow_implicit_invocation: false
