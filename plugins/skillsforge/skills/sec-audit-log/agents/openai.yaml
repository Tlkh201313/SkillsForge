interface:
  display_name: Sec Audit Log
  short_description: Use when doing sec audit log work and you need bounded security-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the sec-audit-log skill for this task.
policy:
  allow_implicit_invocation: false
