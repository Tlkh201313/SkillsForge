interface:
  display_name: Sec Audit Log
  short_description: Use when doing sec audit log work for permission, privacy, boundary, dependency, and threat-model work and you need asset, actor, boundary, failure path, mitigation, and verification evidence before c
  default_prompt: Use the sec-audit-log skill for this task.
policy:
  allow_implicit_invocation: false
