interface:
  display_name: Lang Yaml
  short_description: Use when doing lang yaml work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-yaml skill for this task.
policy:
  allow_implicit_invocation: false
