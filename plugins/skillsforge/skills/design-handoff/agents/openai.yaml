interface:
  display_name: Design Handoff
  short_description: Use when doing design handoff work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-handoff skill for this task.
policy:
  allow_implicit_invocation: false
