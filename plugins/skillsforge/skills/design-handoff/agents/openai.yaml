interface:
  display_name: Design Handoff
  short_description: Use when doing design handoff work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility check,
  default_prompt: Use the design-handoff skill for this task.
policy:
  allow_implicit_invocation: false
