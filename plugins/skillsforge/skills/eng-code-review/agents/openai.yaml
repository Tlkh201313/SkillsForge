interface:
  display_name: Eng Code Review
  short_description: Use when doing eng code review work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-code-review skill for this task.
policy:
  allow_implicit_invocation: false
