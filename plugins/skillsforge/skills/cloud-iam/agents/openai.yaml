interface:
  display_name: Cloud Iam
  short_description: Use when doing cloud iam work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook note
  default_prompt: Use the cloud-iam skill for this task.
policy:
  allow_implicit_invocation: false
