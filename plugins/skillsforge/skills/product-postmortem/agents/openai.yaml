interface:
  display_name: Product Postmortem
  short_description: Use when doing product postmortem work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-postmortem skill for this task.
policy:
  allow_implicit_invocation: false
