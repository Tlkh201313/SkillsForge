interface:
  display_name: Product Metrics
  short_description: Use when doing product metrics work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-metrics skill for this task.
policy:
  allow_implicit_invocation: false
