interface:
  display_name: Ops Oncall
  short_description: Use when doing ops oncall work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-oncall skill for this task.
policy:
  allow_implicit_invocation: false
