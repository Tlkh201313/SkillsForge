interface:
  display_name: Ops Oncall
  short_description: Use when doing ops oncall work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook not
  default_prompt: Use the ops-oncall skill for this task.
policy:
  allow_implicit_invocation: false
