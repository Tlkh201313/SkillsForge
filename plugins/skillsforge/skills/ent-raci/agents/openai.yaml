interface:
  display_name: Ent Raci
  short_description: Use when doing ent raci work for enterprise-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the ent-raci skill for this task.
policy:
  allow_implicit_invocation: false
