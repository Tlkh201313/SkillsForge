interface:
  display_name: Ent Raci
  short_description: Use when doing ent raci work and you need bounded enterprise-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ent-raci skill for this task.
policy:
  allow_implicit_invocation: false
