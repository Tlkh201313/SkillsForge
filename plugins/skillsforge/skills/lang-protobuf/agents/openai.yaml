interface:
  display_name: Lang Protobuf
  short_description: Use when doing lang protobuf work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-protobuf skill for this task.
policy:
  allow_implicit_invocation: false
