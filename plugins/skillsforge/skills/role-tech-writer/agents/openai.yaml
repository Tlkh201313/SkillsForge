interface:
  display_name: Role Tech Writer
  short_description: Use when doing role tech writer work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-tech-writer skill for this task.
policy:
  allow_implicit_invocation: false
