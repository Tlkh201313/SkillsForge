interface:
  display_name: Role Tech Writer
  short_description: Use when doing role tech writer work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-tech-writer skill for this task.
policy:
  allow_implicit_invocation: false
