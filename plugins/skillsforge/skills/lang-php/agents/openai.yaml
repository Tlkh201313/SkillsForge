interface:
  display_name: Lang Php
  short_description: Use when doing lang php work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-php skill for this task.
policy:
  allow_implicit_invocation: false
