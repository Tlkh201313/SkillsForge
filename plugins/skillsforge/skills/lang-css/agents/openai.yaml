interface:
  display_name: Lang Css
  short_description: Use when doing lang css work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-css skill for this task.
policy:
  allow_implicit_invocation: false
