interface:
  display_name: Lang Css
  short_description: Use when doing lang css work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-css skill for this task.
policy:
  allow_implicit_invocation: false
