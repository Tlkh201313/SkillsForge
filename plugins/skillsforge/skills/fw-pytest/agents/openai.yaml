interface:
  display_name: Fw Pytest
  short_description: Use when doing fw pytest work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-pytest skill for this task.
policy:
  allow_implicit_invocation: false
