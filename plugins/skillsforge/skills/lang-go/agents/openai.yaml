interface:
  display_name: Lang Go
  short_description: Use when doing lang go work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-go skill for this task.
policy:
  allow_implicit_invocation: false
