interface:
  display_name: Lang Go
  short_description: Use when doing lang go work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-go skill for this task.
policy:
  allow_implicit_invocation: false
