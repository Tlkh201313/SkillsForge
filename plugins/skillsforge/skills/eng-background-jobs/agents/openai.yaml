interface:
  display_name: Eng Background Jobs
  short_description: Use when doing eng background jobs work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-background-jobs skill for this task.
policy:
  allow_implicit_invocation: false
