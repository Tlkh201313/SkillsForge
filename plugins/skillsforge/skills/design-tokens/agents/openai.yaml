interface:
  display_name: Design Tokens
  short_description: Use when doing design tokens work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility check,
  default_prompt: Use the design-tokens skill for this task.
policy:
  allow_implicit_invocation: false
