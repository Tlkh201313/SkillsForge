interface:
  display_name: Startup Mvp Scope
  short_description: Use when doing startup mvp scope work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and proo
  default_prompt: Use the startup-mvp-scope skill for this task.
policy:
  allow_implicit_invocation: false
