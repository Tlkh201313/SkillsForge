interface:
  display_name: Startup Demo Script
  short_description: Use when doing startup demo script work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and pr
  default_prompt: Use the startup-demo-script skill for this task.
policy:
  allow_implicit_invocation: false
