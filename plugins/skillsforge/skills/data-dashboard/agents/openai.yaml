interface:
  display_name: Data Dashboard
  short_description: Use when doing data dashboard work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-dashboard skill for this task.
policy:
  allow_implicit_invocation: false
