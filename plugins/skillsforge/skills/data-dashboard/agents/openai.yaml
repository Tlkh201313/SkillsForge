interface:
  display_name: Data Dashboard
  short_description: Use when doing data dashboard work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure path
  default_prompt: Use the data-dashboard skill for this task.
policy:
  allow_implicit_invocation: false
