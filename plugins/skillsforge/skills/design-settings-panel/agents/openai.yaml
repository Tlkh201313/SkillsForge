interface:
  display_name: Design Settings Panel
  short_description: Use when doing design settings panel work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility
  default_prompt: Use the design-settings-panel skill for this task.
policy:
  allow_implicit_invocation: false
