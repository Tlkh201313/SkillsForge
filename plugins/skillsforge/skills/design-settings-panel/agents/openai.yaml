interface:
  display_name: Design Settings Panel
  short_description: Use when doing design settings panel work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-settings-panel skill for this task.
policy:
  allow_implicit_invocation: false
