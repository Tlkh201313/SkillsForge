interface:
  display_name: Docs Changelog
  short_description: Use when doing docs changelog work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-changelog skill for this task.
policy:
  allow_implicit_invocation: false
