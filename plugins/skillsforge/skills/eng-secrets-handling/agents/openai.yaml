interface:
  display_name: Eng Secrets Handling
  short_description: Use when doing eng secrets handling work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollbac
  default_prompt: Use the eng-secrets-handling skill for this task.
policy:
  allow_implicit_invocation: false
