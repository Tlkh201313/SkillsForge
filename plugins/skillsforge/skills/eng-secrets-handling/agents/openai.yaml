interface:
  display_name: Eng Secrets Handling
  short_description: Use when doing eng secrets handling work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-secrets-handling skill for this task.
policy:
  allow_implicit_invocation: false
