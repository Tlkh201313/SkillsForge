interface:
  display_name: Eng Logging
  short_description: Use when doing eng logging work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-logging skill for this task.
policy:
  allow_implicit_invocation: false
