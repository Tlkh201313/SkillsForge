interface:
  display_name: Role Pm
  short_description: Use when doing role pm work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-pm skill for this task.
policy:
  allow_implicit_invocation: false
