interface:
  display_name: Role Pm
  short_description: Use when doing role pm work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-pm skill for this task.
policy:
  allow_implicit_invocation: false
