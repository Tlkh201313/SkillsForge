interface:
  display_name: Role Pm
  short_description: Use when you need role pm in a SkillsForge roles workflow.
  default_prompt: Use the role-pm skill for this task.
policy:
  allow_implicit_invocation: true
