interface:
  display_name: Sec Supply Chain
  short_description: Use when doing sec supply chain work for permission, privacy, boundary, dependency, and threat-model work and you need asset, actor, boundary, failure path, mitigation, and verification evidence befor
  default_prompt: Use the sec-supply-chain skill for this task.
policy:
  allow_implicit_invocation: false
