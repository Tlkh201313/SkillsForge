interface:
  display_name: Fullstack Db Schema
  short_description: Use when doing fullstack db schema work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback
  default_prompt: Use the fullstack-db-schema skill for this task.
policy:
  allow_implicit_invocation: false
