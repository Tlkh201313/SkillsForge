interface:
  display_name: Os Env Doctor
  short_description: Use when doing os env doctor work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-env-doctor skill for this task.
policy:
  allow_implicit_invocation: false
