interface:
  display_name: Os Env Doctor
  short_description: Use when you need os env doctor in a SkillsForge os workflow.
  default_prompt: Use the os-env-doctor skill for this task.
policy:
  allow_implicit_invocation: false
