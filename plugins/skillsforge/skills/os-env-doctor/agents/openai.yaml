interface:
  display_name: Os Env Doctor
  short_description: Use when doing os env doctor work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-env-doctor skill for this task.
policy:
  allow_implicit_invocation: false
