interface:
  display_name: Docs Llms Txt
  short_description: Use when doing docs llms txt work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-llms-txt skill for this task.
policy:
  allow_implicit_invocation: false
