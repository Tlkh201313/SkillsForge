interface:
  display_name: Data Quality
  short_description: Use when doing data quality work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-quality skill for this task.
policy:
  allow_implicit_invocation: false
