interface:
  display_name: Data Quality
  short_description: Use when doing data quality work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure path b
  default_prompt: Use the data-quality skill for this task.
policy:
  allow_implicit_invocation: false
