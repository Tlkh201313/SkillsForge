interface:
  display_name: Eng Migrations
  short_description: Use when doing eng migrations work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback note
  default_prompt: Use the eng-migrations skill for this task.
policy:
  allow_implicit_invocation: false
