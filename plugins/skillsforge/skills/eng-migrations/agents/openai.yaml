interface:
  display_name: Eng Migrations
  short_description: Use when doing eng migrations work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-migrations skill for this task.
policy:
  allow_implicit_invocation: false
