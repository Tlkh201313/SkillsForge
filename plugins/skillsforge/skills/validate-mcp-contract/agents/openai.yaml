interface:
  display_name: Validate Mcp Contract
  short_description: Use when doing validate mcp contract work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, and unr
  default_prompt: Use the validate-mcp-contract skill for this task.
policy:
  allow_implicit_invocation: false
