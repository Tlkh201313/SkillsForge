interface:
  display_name: Cloud Networking
  short_description: Use when doing cloud networking work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbo
  default_prompt: Use the cloud-networking skill for this task.
policy:
  allow_implicit_invocation: false
