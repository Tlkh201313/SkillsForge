interface:
  display_name: Cloud Networking
  short_description: Use when doing cloud networking work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-networking skill for this task.
policy:
  allow_implicit_invocation: false
