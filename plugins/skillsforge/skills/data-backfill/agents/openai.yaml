interface:
  display_name: Data Backfill
  short_description: Use when doing data backfill work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure path
  default_prompt: Use the data-backfill skill for this task.
policy:
  allow_implicit_invocation: false
