interface:
  display_name: Test A11y
  short_description: Use when doing test a11y work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-a11y skill for this task.
policy:
  allow_implicit_invocation: false
