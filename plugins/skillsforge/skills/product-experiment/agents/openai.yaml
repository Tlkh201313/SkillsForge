interface:
  display_name: Product Experiment
  short_description: Use when doing product experiment work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and pro
  default_prompt: Use the product-experiment skill for this task.
policy:
  allow_implicit_invocation: false
