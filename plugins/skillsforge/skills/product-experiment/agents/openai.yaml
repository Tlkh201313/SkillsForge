interface:
  display_name: Product Experiment
  short_description: Use when doing product experiment work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-experiment skill for this task.
policy:
  allow_implicit_invocation: false
