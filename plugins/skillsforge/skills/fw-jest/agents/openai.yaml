interface:
  display_name: Fw Jest
  short_description: Use when doing fw jest work for framework-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fw-jest skill for this task.
policy:
  allow_implicit_invocation: false
