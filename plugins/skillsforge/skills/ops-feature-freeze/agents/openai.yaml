interface:
  display_name: Ops Feature Freeze
  short_description: Use when doing ops feature freeze work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and run
  default_prompt: Use the ops-feature-freeze skill for this task.
policy:
  allow_implicit_invocation: false
