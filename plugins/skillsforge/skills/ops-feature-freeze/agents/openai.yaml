interface:
  display_name: Ops Feature Freeze
  short_description: Use when doing ops feature freeze work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-feature-freeze skill for this task.
policy:
  allow_implicit_invocation: false
