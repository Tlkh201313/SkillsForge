interface:
  display_name: Design Navigation
  short_description: Use when doing design navigation work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-navigation skill for this task.
policy:
  allow_implicit_invocation: false
