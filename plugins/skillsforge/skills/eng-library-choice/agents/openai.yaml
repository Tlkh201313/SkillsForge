interface:
  display_name: Eng Library Choice
  short_description: Use when doing eng library choice work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback
  default_prompt: Use the eng-library-choice skill for this task.
policy:
  allow_implicit_invocation: false
