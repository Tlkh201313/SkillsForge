interface:
  display_name: Design Onboarding Flow
  short_description: Use when doing design onboarding flow work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-onboarding-flow skill for this task.
policy:
  allow_implicit_invocation: false
