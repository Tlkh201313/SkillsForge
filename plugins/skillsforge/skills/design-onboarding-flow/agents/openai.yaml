interface:
  display_name: Design Onboarding Flow
  short_description: Use when doing design onboarding flow work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibilit
  default_prompt: Use the design-onboarding-flow skill for this task.
policy:
  allow_implicit_invocation: false
