interface:
  display_name: Role Sre
  short_description: Use when doing role sre work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-sre skill for this task.
policy:
  allow_implicit_invocation: false
