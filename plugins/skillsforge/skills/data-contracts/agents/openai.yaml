interface:
  display_name: Data Contracts
  short_description: Use when doing data contracts work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-contracts skill for this task.
policy:
  allow_implicit_invocation: false
