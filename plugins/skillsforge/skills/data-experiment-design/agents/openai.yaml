interface:
  display_name: Data Experiment Design
  short_description: Use when doing data experiment design work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and fail
  default_prompt: Use the data-experiment-design skill for this task.
policy:
  allow_implicit_invocation: false
