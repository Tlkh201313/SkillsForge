interface:
  display_name: Data Experiment Design
  short_description: Use when doing data experiment design work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-experiment-design skill for this task.
policy:
  allow_implicit_invocation: false
