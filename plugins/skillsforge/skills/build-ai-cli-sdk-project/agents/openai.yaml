interface:
  display_name: Build Ai Cli Sdk Project
  short_description: Use when doing build ai cli sdk project work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation p
  default_prompt: Use the build-ai-cli-sdk-project skill for this task.
policy:
  allow_implicit_invocation: false
