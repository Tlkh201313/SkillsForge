interface:
  display_name: Design Admin Console
  short_description: Use when doing design admin console work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-admin-console skill for this task.
policy:
  allow_implicit_invocation: false
