interface:
  display_name: Fw React
  short_description: Use when doing fw react work for framework-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fw-react skill for this task.
policy:
  allow_implicit_invocation: false
