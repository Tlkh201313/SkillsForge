interface:
  display_name: Fw React
  short_description: Use when doing fw react work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-react skill for this task.
policy:
  allow_implicit_invocation: false
