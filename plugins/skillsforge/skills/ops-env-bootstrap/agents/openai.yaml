interface:
  display_name: Ops Env Bootstrap
  short_description: Use when doing ops env bootstrap work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runb
  default_prompt: Use the ops-env-bootstrap skill for this task.
policy:
  allow_implicit_invocation: false
