interface:
  display_name: Test Fixture
  short_description: Use when doing test fixture work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-fixture skill for this task.
policy:
  allow_implicit_invocation: false
