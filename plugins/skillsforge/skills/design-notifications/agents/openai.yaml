interface:
  display_name: Design Notifications
  short_description: Use when doing design notifications work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-notifications skill for this task.
policy:
  allow_implicit_invocation: false
