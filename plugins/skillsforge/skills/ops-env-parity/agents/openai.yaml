interface:
  display_name: Ops Env Parity
  short_description: Use when doing ops env parity work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook
  default_prompt: Use the ops-env-parity skill for this task.
policy:
  allow_implicit_invocation: false
