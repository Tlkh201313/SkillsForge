interface:
  display_name: Ops Env Parity
  short_description: Use when doing ops env parity work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-env-parity skill for this task.
policy:
  allow_implicit_invocation: false
