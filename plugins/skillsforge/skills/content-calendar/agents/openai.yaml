interface:
  display_name: Content Calendar
  short_description: Use when doing content calendar work for agent-facing docs, launch media, demo proof, and content production and you need audience, source facts, outline, asset path, claim proof, and publishing check
  default_prompt: Use the content-calendar skill for this task.
policy:
  allow_implicit_invocation: false
