interface:
  display_name: Eng Pagination
  short_description: Use when doing eng pagination work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-pagination skill for this task.
policy:
  allow_implicit_invocation: false
