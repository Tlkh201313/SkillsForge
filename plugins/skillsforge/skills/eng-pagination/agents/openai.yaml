interface:
  display_name: Eng Pagination
  short_description: Use when doing eng pagination work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback note
  default_prompt: Use the eng-pagination skill for this task.
policy:
  allow_implicit_invocation: false
