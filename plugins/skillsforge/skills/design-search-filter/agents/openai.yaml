interface:
  display_name: Design Search Filter
  short_description: Use when doing design search filter work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-search-filter skill for this task.
policy:
  allow_implicit_invocation: false
