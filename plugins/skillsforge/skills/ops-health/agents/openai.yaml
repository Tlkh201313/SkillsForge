interface:
  display_name: Ops Health
  short_description: Use when doing ops health work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook not
  default_prompt: Use the ops-health skill for this task.
policy:
  allow_implicit_invocation: false
