interface:
  display_name: Ops Health
  short_description: Use when doing ops health work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-health skill for this task.
policy:
  allow_implicit_invocation: false
