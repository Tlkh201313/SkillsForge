interface:
  display_name: Lang R
  short_description: Use when doing lang r work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-r skill for this task.
policy:
  allow_implicit_invocation: false
