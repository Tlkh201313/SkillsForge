interface:
  display_name: Build Hermes Agent Pack
  short_description: Use when doing build hermes agent pack work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation pa
  default_prompt: Use the build-hermes-agent-pack skill for this task.
policy:
  allow_implicit_invocation: false
