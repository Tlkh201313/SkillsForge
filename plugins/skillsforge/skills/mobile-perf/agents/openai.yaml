interface:
  display_name: Mobile Perf
  short_description: Use when doing mobile perf work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-perf skill for this task.
policy:
  allow_implicit_invocation: false
