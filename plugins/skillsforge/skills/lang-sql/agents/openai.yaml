interface:
  display_name: Lang Sql
  short_description: Use when doing lang sql work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-sql skill for this task.
policy:
  allow_implicit_invocation: false
