interface:
  display_name: Test Visual
  short_description: Use when doing test visual work for testing-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the test-visual skill for this task.
policy:
  allow_implicit_invocation: false
