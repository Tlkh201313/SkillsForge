interface:
  display_name: Test Visual
  short_description: Use when doing test visual work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-visual skill for this task.
policy:
  allow_implicit_invocation: false
