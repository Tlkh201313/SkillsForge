interface:
  display_name: Product Persona
  short_description: Use when doing product persona work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-persona skill for this task.
policy:
  allow_implicit_invocation: false
