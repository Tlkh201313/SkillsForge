interface:
  display_name: Product Persona
  short_description: Use when doing product persona work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and proof
  default_prompt: Use the product-persona skill for this task.
policy:
  allow_implicit_invocation: false
