interface:
  display_name: Role Devex Review
  short_description: Use when doing role devex review work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-devex-review skill for this task.
policy:
  allow_implicit_invocation: false
