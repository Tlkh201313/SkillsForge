interface:
  display_name: Mobile Push
  short_description: Use when doing mobile push work for mobile-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the mobile-push skill for this task.
policy:
  allow_implicit_invocation: false
