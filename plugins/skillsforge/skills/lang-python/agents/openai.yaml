interface:
  display_name: Lang Python
  short_description: Use when doing lang python work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-python skill for this task.
policy:
  allow_implicit_invocation: false
