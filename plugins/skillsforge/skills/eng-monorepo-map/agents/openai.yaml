interface:
  display_name: Eng Monorepo Map
  short_description: Use when doing eng monorepo map work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback no
  default_prompt: Use the eng-monorepo-map skill for this task.
policy:
  allow_implicit_invocation: false
