interface:
  display_name: Design Component States
  short_description: Use when doing design component states work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibili
  default_prompt: Use the design-component-states skill for this task.
policy:
  allow_implicit_invocation: false
