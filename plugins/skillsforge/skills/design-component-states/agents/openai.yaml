interface:
  display_name: Design Component States
  short_description: Use when doing design component states work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-component-states skill for this task.
policy:
  allow_implicit_invocation: false
