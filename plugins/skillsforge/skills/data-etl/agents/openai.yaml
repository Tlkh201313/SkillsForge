interface:
  display_name: Data Etl
  short_description: Use when doing data etl work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure path befor
  default_prompt: Use the data-etl skill for this task.
policy:
  allow_implicit_invocation: false
