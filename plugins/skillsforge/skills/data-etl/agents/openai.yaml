interface:
  display_name: Data Etl
  short_description: Use when doing data etl work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-etl skill for this task.
policy:
  allow_implicit_invocation: false
