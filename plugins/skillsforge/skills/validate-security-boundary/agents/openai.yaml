interface:
  display_name: Validate Security Boundary
  short_description: Use when doing validate security boundary work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, an
  default_prompt: Use the validate-security-boundary skill for this task.
policy:
  allow_implicit_invocation: false
