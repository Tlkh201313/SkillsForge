interface:
  display_name: Design Brand
  short_description: Use when doing design brand work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility check, a
  default_prompt: Use the design-brand skill for this task.
policy:
  allow_implicit_invocation: false
