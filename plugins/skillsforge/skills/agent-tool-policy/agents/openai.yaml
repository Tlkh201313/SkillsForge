interface:
  display_name: Agent Tool Policy
  short_description: Use when you need agent tool policy in a SkillsForge agentic workflow.
  default_prompt: Use the agent-tool-policy skill for this task.
policy:
  allow_implicit_invocation: true
