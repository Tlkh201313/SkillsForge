interface:
  display_name: Agent Tool Policy
  short_description: Use when doing agent tool policy work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-tool-policy skill for this task.
policy:
  allow_implicit_invocation: false
