interface:
  display_name: Agent Tool Policy
  short_description: Use when doing agent tool policy work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay path, a
  default_prompt: Use the agent-tool-policy skill for this task.
policy:
  allow_implicit_invocation: false
