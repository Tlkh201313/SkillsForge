interface:
  display_name: Data Governance
  short_description: Use when doing data governance work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure pat
  default_prompt: Use the data-governance skill for this task.
policy:
  allow_implicit_invocation: false
