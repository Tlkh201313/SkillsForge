interface:
  display_name: Media Compression
  short_description: Use when doing media compression work and you need bounded media-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the media-compression skill for this task.
policy:
  allow_implicit_invocation: false
