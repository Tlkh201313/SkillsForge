interface:
  display_name: Fw Fastapi
  short_description: Use when doing fw fastapi work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-fastapi skill for this task.
policy:
  allow_implicit_invocation: false
