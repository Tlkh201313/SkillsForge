interface:
  display_name: Lang Terraform
  short_description: Use when doing lang terraform work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-terraform skill for this task.
policy:
  allow_implicit_invocation: false
