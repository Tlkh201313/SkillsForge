interface:
  display_name: Lang Terraform
  short_description: Use when doing lang terraform work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-terraform skill for this task.
policy:
  allow_implicit_invocation: false
