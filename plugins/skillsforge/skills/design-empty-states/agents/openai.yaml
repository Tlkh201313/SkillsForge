interface:
  display_name: Design Empty States
  short_description: Use when doing design empty states work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-empty-states skill for this task.
policy:
  allow_implicit_invocation: false
