interface:
  display_name: Lang Lua
  short_description: Use when doing lang lua work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-lua skill for this task.
policy:
  allow_implicit_invocation: false
