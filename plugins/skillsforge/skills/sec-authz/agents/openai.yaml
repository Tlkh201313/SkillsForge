interface:
  display_name: Sec Authz
  short_description: Use when doing sec authz work and you need bounded security-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the sec-authz skill for this task.
policy:
  allow_implicit_invocation: false
