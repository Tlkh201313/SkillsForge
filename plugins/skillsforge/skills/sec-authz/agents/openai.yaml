interface:
  display_name: Sec Authz
  short_description: Use when doing sec authz work for permission, privacy, boundary, dependency, and threat-model work and you need asset, actor, boundary, failure path, mitigation, and verification evidence before claim
  default_prompt: Use the sec-authz skill for this task.
policy:
  allow_implicit_invocation: false
