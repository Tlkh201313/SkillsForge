interface:
  display_name: Lang Powershell
  short_description: Use when doing lang powershell work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-powershell skill for this task.
policy:
  allow_implicit_invocation: false
