interface:
  display_name: Os Shell Modernize
  short_description: Use when you need os shell modernize in a SkillsForge os workflow.
  default_prompt: Use the os-shell-modernize skill for this task.
policy:
  allow_implicit_invocation: false
