interface:
  display_name: Os Shell Modernize
  short_description: Use when doing os shell modernize work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-shell-modernize skill for this task.
policy:
  allow_implicit_invocation: false
