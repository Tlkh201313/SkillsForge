interface:
  display_name: Os Shell Modernize
  short_description: Use when doing os shell modernize work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-shell-modernize skill for this task.
policy:
  allow_implicit_invocation: false
