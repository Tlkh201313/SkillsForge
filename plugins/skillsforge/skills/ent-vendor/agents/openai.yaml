interface:
  display_name: Ent Vendor
  short_description: Use when doing ent vendor work for enterprise-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the ent-vendor skill for this task.
policy:
  allow_implicit_invocation: false
