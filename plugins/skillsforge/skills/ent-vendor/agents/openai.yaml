interface:
  display_name: Ent Vendor
  short_description: Use when doing ent vendor work and you need bounded enterprise-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ent-vendor skill for this task.
policy:
  allow_implicit_invocation: false
