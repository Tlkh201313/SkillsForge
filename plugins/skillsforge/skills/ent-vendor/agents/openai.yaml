interface:
  display_name: Ent Vendor
  short_description: Use when you need ent vendor in a SkillsForge enterprise workflow.
  default_prompt: Use the ent-vendor skill for this task.
policy:
  allow_implicit_invocation: true
