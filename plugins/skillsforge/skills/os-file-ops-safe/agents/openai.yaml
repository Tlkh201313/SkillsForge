interface:
  display_name: Os File Ops Safe
  short_description: Use when doing os file ops safe work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-file-ops-safe skill for this task.
policy:
  allow_implicit_invocation: false
