interface:
  display_name: Os File Ops Safe
  short_description: Use when doing os file ops safe work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-file-ops-safe skill for this task.
policy:
  allow_implicit_invocation: false
