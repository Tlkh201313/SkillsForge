interface:
  display_name: Content Longform
  short_description: Use when doing content longform work and you need bounded content-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the content-longform skill for this task.
policy:
  allow_implicit_invocation: false
