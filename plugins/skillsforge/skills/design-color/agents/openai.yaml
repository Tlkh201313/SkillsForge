interface:
  display_name: Design Color
  short_description: Use when doing design color work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility check, a
  default_prompt: Use the design-color skill for this task.
policy:
  allow_implicit_invocation: false
