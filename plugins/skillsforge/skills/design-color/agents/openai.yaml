interface:
  display_name: Design Color
  short_description: Use when doing design color work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-color skill for this task.
policy:
  allow_implicit_invocation: false
