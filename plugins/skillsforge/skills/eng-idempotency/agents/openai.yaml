interface:
  display_name: Eng Idempotency
  short_description: Use when doing eng idempotency work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback not
  default_prompt: Use the eng-idempotency skill for this task.
policy:
  allow_implicit_invocation: false
