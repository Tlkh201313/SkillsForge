interface:
  display_name: Fw Svelte
  short_description: Use when doing fw svelte work for framework-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fw-svelte skill for this task.
policy:
  allow_implicit_invocation: false
