interface:
  display_name: Fw Svelte
  short_description: Use when doing fw svelte work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-svelte skill for this task.
policy:
  allow_implicit_invocation: false
