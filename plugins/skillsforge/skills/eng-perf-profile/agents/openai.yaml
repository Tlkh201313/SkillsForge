interface:
  display_name: Eng Perf Profile
  short_description: Use when doing eng perf profile work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback no
  default_prompt: Use the eng-perf-profile skill for this task.
policy:
  allow_implicit_invocation: false
