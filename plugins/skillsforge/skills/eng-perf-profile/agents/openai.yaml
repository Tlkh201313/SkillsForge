interface:
  display_name: Eng Perf Profile
  short_description: Use when doing eng perf profile work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-perf-profile skill for this task.
policy:
  allow_implicit_invocation: false
