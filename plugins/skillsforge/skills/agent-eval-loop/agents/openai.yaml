interface:
  display_name: Agent Eval Loop
  short_description: Use when doing agent eval loop work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay path, and
  default_prompt: Use the agent-eval-loop skill for this task.
policy:
  allow_implicit_invocation: false
