interface:
  display_name: Agent Eval Loop
  short_description: Use when doing agent eval loop work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-eval-loop skill for this task.
policy:
  allow_implicit_invocation: false
