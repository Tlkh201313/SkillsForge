interface:
  display_name: Agent Eval Loop
  short_description: Use when you need agent eval loop in a SkillsForge agentic workflow.
  default_prompt: Use the agent-eval-loop skill for this task.
policy:
  allow_implicit_invocation: true
