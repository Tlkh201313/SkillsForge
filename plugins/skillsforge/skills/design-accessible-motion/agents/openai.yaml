interface:
  display_name: Design Accessible Motion
  short_description: Use when doing design accessible motion work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibil
  default_prompt: Use the design-accessible-motion skill for this task.
policy:
  allow_implicit_invocation: false
