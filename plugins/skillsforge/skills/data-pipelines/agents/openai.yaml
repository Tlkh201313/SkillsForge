interface:
  display_name: Data Pipelines
  short_description: Use when doing data pipelines work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-pipelines skill for this task.
policy:
  allow_implicit_invocation: false
