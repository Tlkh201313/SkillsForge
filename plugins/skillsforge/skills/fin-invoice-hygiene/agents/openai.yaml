interface:
  display_name: Fin Invoice Hygiene
  short_description: Use when doing fin invoice hygiene work and you need bounded finance-lite-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fin-invoice-hygiene skill for this task.
policy:
  allow_implicit_invocation: false
