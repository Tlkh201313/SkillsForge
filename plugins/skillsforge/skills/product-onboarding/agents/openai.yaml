interface:
  display_name: Product Onboarding
  short_description: Use when doing product onboarding work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and pro
  default_prompt: Use the product-onboarding skill for this task.
policy:
  allow_implicit_invocation: false
