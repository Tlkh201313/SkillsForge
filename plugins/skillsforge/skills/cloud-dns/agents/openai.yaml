interface:
  display_name: Cloud Dns
  short_description: Use when doing cloud dns work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-dns skill for this task.
policy:
  allow_implicit_invocation: false
