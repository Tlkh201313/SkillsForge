interface:
  display_name: Cloud Dns
  short_description: Use when doing cloud dns work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook note
  default_prompt: Use the cloud-dns skill for this task.
policy:
  allow_implicit_invocation: false
