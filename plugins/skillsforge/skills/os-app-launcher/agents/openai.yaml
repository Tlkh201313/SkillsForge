interface:
  display_name: Os App Launcher
  short_description: Use when doing os app launcher work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-app-launcher skill for this task.
policy:
  allow_implicit_invocation: false
