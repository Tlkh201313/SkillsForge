interface:
  display_name: Os App Launcher
  short_description: Use when doing os app launcher work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-app-launcher skill for this task.
policy:
  allow_implicit_invocation: false
