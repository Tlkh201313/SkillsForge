interface:
  display_name: Test Mutation
  short_description: Use when doing test mutation work for testing-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the test-mutation skill for this task.
policy:
  allow_implicit_invocation: false
