interface:
  display_name: Test Mutation
  short_description: Use when doing test mutation work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-mutation skill for this task.
policy:
  allow_implicit_invocation: false
