interface:
  display_name: Test Property
  short_description: Use when doing test property work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-property skill for this task.
policy:
  allow_implicit_invocation: false
