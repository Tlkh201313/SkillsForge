interface:
  display_name: Agent Replay
  short_description: Use when you need agent replay in a SkillsForge agentic workflow.
  default_prompt: Use the agent-replay skill for this task.
policy:
  allow_implicit_invocation: true
