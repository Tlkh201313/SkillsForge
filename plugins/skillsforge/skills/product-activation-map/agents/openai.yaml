interface:
  display_name: Product Activation Map
  short_description: Use when doing product activation map work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and
  default_prompt: Use the product-activation-map skill for this task.
policy:
  allow_implicit_invocation: false
