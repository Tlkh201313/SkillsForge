interface:
  display_name: Mobile Store Listing
  short_description: Use when doing mobile store listing work for mobile-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the mobile-store-listing skill for this task.
policy:
  allow_implicit_invocation: false
