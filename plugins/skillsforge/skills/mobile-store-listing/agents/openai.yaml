interface:
  display_name: Mobile Store Listing
  short_description: Use when doing mobile store listing work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-store-listing skill for this task.
policy:
  allow_implicit_invocation: false
