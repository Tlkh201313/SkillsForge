interface:
  display_name: Design Error Recovery
  short_description: Use when doing design error recovery work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-error-recovery skill for this task.
policy:
  allow_implicit_invocation: false
