interface:
  display_name: Agentic Workflow Replay
  short_description: Use when doing agentic workflow replay work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay p
  default_prompt: Use the agentic-workflow-replay skill for this task.
policy:
  allow_implicit_invocation: false
