interface:
  display_name: Validate Release Readiness
  short_description: Use when doing validate release readiness work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, an
  default_prompt: Use the validate-release-readiness skill for this task.
policy:
  allow_implicit_invocation: false
