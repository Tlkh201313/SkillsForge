interface:
  display_name: Os Agent Terminal
  short_description: Use when doing os agent terminal work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-agent-terminal skill for this task.
policy:
  allow_implicit_invocation: false
