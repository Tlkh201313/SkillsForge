interface:
  display_name: Os Agent Terminal
  short_description: Use when you need os agent terminal in a SkillsForge os workflow.
  default_prompt: Use the os-agent-terminal skill for this task.
policy:
  allow_implicit_invocation: false
