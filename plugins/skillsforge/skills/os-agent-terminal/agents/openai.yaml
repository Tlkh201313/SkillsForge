interface:
  display_name: Os Agent Terminal
  short_description: Use when doing os agent terminal work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-agent-terminal skill for this task.
policy:
  allow_implicit_invocation: false
