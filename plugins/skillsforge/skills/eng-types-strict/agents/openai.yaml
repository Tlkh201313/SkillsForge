interface:
  display_name: Eng Types Strict
  short_description: Use when doing eng types strict work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback no
  default_prompt: Use the eng-types-strict skill for this task.
policy:
  allow_implicit_invocation: false
