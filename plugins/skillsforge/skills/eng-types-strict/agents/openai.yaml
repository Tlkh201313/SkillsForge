interface:
  display_name: Eng Types Strict
  short_description: Use when doing eng types strict work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-types-strict skill for this task.
policy:
  allow_implicit_invocation: false
