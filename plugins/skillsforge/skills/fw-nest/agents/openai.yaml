interface:
  display_name: Fw Nest
  short_description: Use when doing fw nest work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-nest skill for this task.
policy:
  allow_implicit_invocation: false
