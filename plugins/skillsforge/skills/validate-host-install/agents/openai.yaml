interface:
  display_name: Validate Host Install
  short_description: Use when doing validate host install work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, and unr
  default_prompt: Use the validate-host-install skill for this task.
policy:
  allow_implicit_invocation: false
