interface:
  display_name: Eng Contracts
  short_description: Use when doing eng contracts work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-contracts skill for this task.
policy:
  allow_implicit_invocation: false
