interface:
  display_name: Data Streaming
  short_description: Use when doing data streaming work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-streaming skill for this task.
policy:
  allow_implicit_invocation: false
