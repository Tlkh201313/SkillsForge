interface:
  display_name: Product Onboarding Audit
  short_description: Use when doing product onboarding audit work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, a
  default_prompt: Use the product-onboarding-audit skill for this task.
policy:
  allow_implicit_invocation: false
