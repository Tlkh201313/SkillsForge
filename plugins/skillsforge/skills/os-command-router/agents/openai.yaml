interface:
  display_name: Os Command Router
  short_description: Use when doing os command router work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-command-router skill for this task.
policy:
  allow_implicit_invocation: false
