interface:
  display_name: Lang Json Schema
  short_description: Use when doing lang json schema work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-json-schema skill for this task.
policy:
  allow_implicit_invocation: false
