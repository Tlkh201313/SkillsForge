interface:
  display_name: Lang Json Schema
  short_description: Use when you need lang json schema in a SkillsForge lang workflow.
  default_prompt: Use the lang-json-schema skill for this task.
policy:
  allow_implicit_invocation: false
