interface:
  display_name: Docs Runbook
  short_description: Use when doing docs runbook work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-runbook skill for this task.
policy:
  allow_implicit_invocation: false
