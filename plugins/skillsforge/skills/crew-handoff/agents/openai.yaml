interface:
  display_name: Crew Handoff
  short_description: Use when doing crew handoff work for lifecycle-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the crew-handoff skill for this task.
policy:
  allow_implicit_invocation: false
