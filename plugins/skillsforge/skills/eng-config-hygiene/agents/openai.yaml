interface:
  display_name: Eng Config Hygiene
  short_description: Use when doing eng config hygiene work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback
  default_prompt: Use the eng-config-hygiene skill for this task.
policy:
  allow_implicit_invocation: false
