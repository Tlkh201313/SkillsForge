interface:
  display_name: Eng Config Hygiene
  short_description: Use when doing eng config hygiene work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-config-hygiene skill for this task.
policy:
  allow_implicit_invocation: false
