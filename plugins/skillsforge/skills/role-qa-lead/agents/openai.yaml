interface:
  display_name: Role Qa Lead
  short_description: Use when doing role qa lead work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-qa-lead skill for this task.
policy:
  allow_implicit_invocation: false
