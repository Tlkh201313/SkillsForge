interface:
  display_name: Role Qa Lead
  short_description: Use when doing role qa lead work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-qa-lead skill for this task.
policy:
  allow_implicit_invocation: false
