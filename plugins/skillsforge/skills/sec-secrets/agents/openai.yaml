interface:
  display_name: Sec Secrets
  short_description: Use when you need sec secrets in a SkillsForge security workflow.
  default_prompt: Use the sec-secrets skill for this task.
policy:
  allow_implicit_invocation: true
