interface:
  display_name: Sec Secrets
  short_description: Use when doing sec secrets work and you need bounded security-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the sec-secrets skill for this task.
policy:
  allow_implicit_invocation: false
