interface:
  display_name: Validate Plugin Package
  short_description: Use when doing validate plugin package work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, and u
  default_prompt: Use the validate-plugin-package skill for this task.
policy:
  allow_implicit_invocation: false
