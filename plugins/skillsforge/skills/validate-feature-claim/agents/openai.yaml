interface:
  display_name: Validate Feature Claim
  short_description: Use when doing validate feature claim work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, and un
  default_prompt: Use the validate-feature-claim skill for this task.
policy:
  allow_implicit_invocation: false
