interface:
  display_name: Test Regression
  short_description: Use when doing test regression work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-regression skill for this task.
policy:
  allow_implicit_invocation: false
