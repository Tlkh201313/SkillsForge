interface:
  display_name: Validate Workflow Runbook
  short_description: Use when doing validate workflow runbook work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, and
  default_prompt: Use the validate-workflow-runbook skill for this task.
policy:
  allow_implicit_invocation: false
