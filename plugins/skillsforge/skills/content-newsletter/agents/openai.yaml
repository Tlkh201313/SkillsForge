interface:
  display_name: Content Newsletter
  short_description: Use when doing content newsletter work and you need bounded content-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the content-newsletter skill for this task.
policy:
  allow_implicit_invocation: false
