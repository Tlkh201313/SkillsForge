interface:
  display_name: Data Metrics Dict
  short_description: Use when doing data metrics dict work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-metrics-dict skill for this task.
policy:
  allow_implicit_invocation: false
