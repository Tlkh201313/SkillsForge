interface:
  display_name: Media Brand Asset
  short_description: Use when doing media brand asset work and you need bounded media-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the media-brand-asset skill for this task.
policy:
  allow_implicit_invocation: false
