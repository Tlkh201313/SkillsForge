interface:
  display_name: Lang Wasm
  short_description: Use when doing lang wasm work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-wasm skill for this task.
policy:
  allow_implicit_invocation: false
