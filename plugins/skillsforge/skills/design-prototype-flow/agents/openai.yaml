interface:
  display_name: Design Prototype Flow
  short_description: Use when doing design prototype flow work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-prototype-flow skill for this task.
policy:
  allow_implicit_invocation: false
