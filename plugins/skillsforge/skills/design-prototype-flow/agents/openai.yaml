interface:
  display_name: Design Prototype Flow
  short_description: Use when doing design prototype flow work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility
  default_prompt: Use the design-prototype-flow skill for this task.
policy:
  allow_implicit_invocation: false
