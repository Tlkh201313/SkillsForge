interface:
  display_name: Docs Migration
  short_description: Use when doing docs migration work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-migration skill for this task.
policy:
  allow_implicit_invocation: false
