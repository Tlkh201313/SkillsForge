interface:
  display_name: Os Process Port Doctor
  short_description: Use when doing os process port doctor work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-process-port-doctor skill for this task.
policy:
  allow_implicit_invocation: false
