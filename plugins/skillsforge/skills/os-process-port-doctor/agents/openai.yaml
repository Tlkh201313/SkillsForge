interface:
  display_name: Os Process Port Doctor
  short_description: Use when doing os process port doctor work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-process-port-doctor skill for this task.
policy:
  allow_implicit_invocation: false
