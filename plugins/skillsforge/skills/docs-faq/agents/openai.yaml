interface:
  display_name: Docs Faq
  short_description: Use when doing docs faq work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-faq skill for this task.
policy:
  allow_implicit_invocation: false
