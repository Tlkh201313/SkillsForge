interface:
  display_name: Cloud Docker
  short_description: Use when doing cloud docker work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook n
  default_prompt: Use the cloud-docker skill for this task.
policy:
  allow_implicit_invocation: false
