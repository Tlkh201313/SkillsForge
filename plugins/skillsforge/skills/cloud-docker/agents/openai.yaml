interface:
  display_name: Cloud Docker
  short_description: Use when doing cloud docker work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-docker skill for this task.
policy:
  allow_implicit_invocation: false
