interface:
  display_name: Startup Landing Offer
  short_description: Use when doing startup landing offer work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and
  default_prompt: Use the startup-landing-offer skill for this task.
policy:
  allow_implicit_invocation: false
