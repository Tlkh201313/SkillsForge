interface:
  display_name: Fin Burn
  short_description: Use when doing fin burn work and you need bounded finance-lite-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fin-burn skill for this task.
policy:
  allow_implicit_invocation: false
