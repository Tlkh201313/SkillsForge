interface:
  display_name: Product Launch
  short_description: Use when doing product launch work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-launch skill for this task.
policy:
  allow_implicit_invocation: false
