interface:
  display_name: Fw Flutter
  short_description: Use when doing fw flutter work for framework-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fw-flutter skill for this task.
policy:
  allow_implicit_invocation: false
