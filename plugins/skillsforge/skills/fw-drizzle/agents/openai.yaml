interface:
  display_name: Fw Drizzle
  short_description: Use when doing fw drizzle work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-drizzle skill for this task.
policy:
  allow_implicit_invocation: false
