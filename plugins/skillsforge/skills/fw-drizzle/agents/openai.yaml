interface:
  display_name: Fw Drizzle
  short_description: Use when doing fw drizzle work for framework-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fw-drizzle skill for this task.
policy:
  allow_implicit_invocation: false
