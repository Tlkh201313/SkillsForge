interface:
  display_name: Data Warehouse
  short_description: Use when doing data warehouse work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-warehouse skill for this task.
policy:
  allow_implicit_invocation: false
