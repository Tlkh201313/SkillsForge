interface:
  display_name: Growth Referral
  short_description: Use when doing growth referral work and you need bounded growth-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the growth-referral skill for this task.
policy:
  allow_implicit_invocation: false
