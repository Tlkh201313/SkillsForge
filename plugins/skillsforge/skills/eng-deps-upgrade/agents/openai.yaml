interface:
  display_name: Eng Deps Upgrade
  short_description: Use when doing eng deps upgrade work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-deps-upgrade skill for this task.
policy:
  allow_implicit_invocation: false
