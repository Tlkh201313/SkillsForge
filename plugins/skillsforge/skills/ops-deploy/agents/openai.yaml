interface:
  display_name: Ops Deploy
  short_description: Use when doing ops deploy work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-deploy skill for this task.
policy:
  allow_implicit_invocation: false
