interface:
  display_name: Test Load
  short_description: Use when doing test load work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-load skill for this task.
policy:
  allow_implicit_invocation: false
