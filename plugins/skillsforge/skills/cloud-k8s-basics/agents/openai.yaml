interface:
  display_name: Cloud K8s Basics
  short_description: Use when doing cloud k8s basics work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbo
  default_prompt: Use the cloud-k8s-basics skill for this task.
policy:
  allow_implicit_invocation: false
