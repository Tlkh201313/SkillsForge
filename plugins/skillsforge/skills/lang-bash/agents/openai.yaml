interface:
  display_name: Lang Bash
  short_description: Use when doing lang bash work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-bash skill for this task.
policy:
  allow_implicit_invocation: false
