interface:
  display_name: Role Design Review
  short_description: Use when doing role design review work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-design-review skill for this task.
policy:
  allow_implicit_invocation: false
