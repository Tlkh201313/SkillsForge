interface:
  display_name: Fw Playwright
  short_description: Use when doing fw playwright work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-playwright skill for this task.
policy:
  allow_implicit_invocation: false
