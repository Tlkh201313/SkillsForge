interface:
  display_name: Cloud Ci Providers
  short_description: Use when doing cloud ci providers work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-ci-providers skill for this task.
policy:
  allow_implicit_invocation: false
