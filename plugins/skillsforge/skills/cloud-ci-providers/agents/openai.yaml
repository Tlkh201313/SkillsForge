interface:
  display_name: Cloud Ci Providers
  short_description: Use when doing cloud ci providers work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and run
  default_prompt: Use the cloud-ci-providers skill for this task.
policy:
  allow_implicit_invocation: false
