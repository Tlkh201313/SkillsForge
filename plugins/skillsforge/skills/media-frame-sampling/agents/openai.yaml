interface:
  display_name: Media Frame Sampling
  short_description: Use when doing media frame sampling work for agent-facing docs, launch media, demo proof, and content production and you need audience, source facts, outline, asset path, claim proof, and publishing c
  default_prompt: Use the media-frame-sampling skill for this task.
policy:
  allow_implicit_invocation: false
