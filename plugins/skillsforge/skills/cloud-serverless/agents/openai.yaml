interface:
  display_name: Cloud Serverless
  short_description: Use when doing cloud serverless work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbo
  default_prompt: Use the cloud-serverless skill for this task.
policy:
  allow_implicit_invocation: false
