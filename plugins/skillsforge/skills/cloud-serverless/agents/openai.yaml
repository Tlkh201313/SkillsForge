interface:
  display_name: Cloud Serverless
  short_description: Use when doing cloud serverless work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-serverless skill for this task.
policy:
  allow_implicit_invocation: false
