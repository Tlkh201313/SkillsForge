interface:
  display_name: Lang Elixir
  short_description: Use when doing lang elixir work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-elixir skill for this task.
policy:
  allow_implicit_invocation: false
