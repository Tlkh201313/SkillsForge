interface:
  display_name: Lang Elixir
  short_description: Use when doing lang elixir work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-elixir skill for this task.
policy:
  allow_implicit_invocation: false
