interface:
  display_name: Lang Regex
  short_description: Use when you need lang regex in a SkillsForge lang workflow.
  default_prompt: Use the lang-regex skill for this task.
policy:
  allow_implicit_invocation: true
