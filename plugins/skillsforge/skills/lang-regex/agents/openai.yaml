interface:
  display_name: Lang Regex
  short_description: Use when doing lang regex work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-regex skill for this task.
policy:
  allow_implicit_invocation: false
