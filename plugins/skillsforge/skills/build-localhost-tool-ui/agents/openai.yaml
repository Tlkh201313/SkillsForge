interface:
  display_name: Build Localhost Tool Ui
  short_description: Use when doing build localhost tool ui work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation pa
  default_prompt: Use the build-localhost-tool-ui skill for this task.
policy:
  allow_implicit_invocation: false
