interface:
  display_name: Build Mcp Server
  short_description: Use when doing build mcp server work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation path befo
  default_prompt: Use the build-mcp-server skill for this task.
policy:
  allow_implicit_invocation: false
