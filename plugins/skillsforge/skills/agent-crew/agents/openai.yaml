interface:
  display_name: Agent Crew
  short_description: Use when doing agent crew work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-crew skill for this task.
policy:
  allow_implicit_invocation: false
