interface:
  display_name: Lang Html
  short_description: Use when doing lang html work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-html skill for this task.
policy:
  allow_implicit_invocation: false
