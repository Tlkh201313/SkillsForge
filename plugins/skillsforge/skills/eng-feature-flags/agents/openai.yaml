interface:
  display_name: Eng Feature Flags
  short_description: Use when doing eng feature flags work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-feature-flags skill for this task.
policy:
  allow_implicit_invocation: false
