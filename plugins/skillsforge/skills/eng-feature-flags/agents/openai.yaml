interface:
  display_name: Eng Feature Flags
  short_description: Use when doing eng feature flags work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback n
  default_prompt: Use the eng-feature-flags skill for this task.
policy:
  allow_implicit_invocation: false
