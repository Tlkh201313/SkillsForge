interface:
  display_name: Cloud Cdn
  short_description: Use when doing cloud cdn work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-cdn skill for this task.
policy:
  allow_implicit_invocation: false
