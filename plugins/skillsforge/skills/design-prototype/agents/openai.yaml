interface:
  display_name: Design Prototype
  short_description: Use when doing design prototype work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility chec
  default_prompt: Use the design-prototype skill for this task.
policy:
  allow_implicit_invocation: false
