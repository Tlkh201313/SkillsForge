interface:
  display_name: Cloud Cost Tags
  short_description: Use when doing cloud cost tags work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-cost-tags skill for this task.
policy:
  allow_implicit_invocation: false
