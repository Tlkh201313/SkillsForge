interface:
  display_name: Cloud Cost Tags
  short_description: Use when doing cloud cost tags work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runboo
  default_prompt: Use the cloud-cost-tags skill for this task.
policy:
  allow_implicit_invocation: false
