interface:
  display_name: Data Schema
  short_description: Use when doing data schema work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-schema skill for this task.
policy:
  allow_implicit_invocation: false
