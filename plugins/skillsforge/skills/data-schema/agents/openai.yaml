interface:
  display_name: Data Schema
  short_description: Use when you need data schema in a SkillsForge data workflow.
  default_prompt: Use the data-schema skill for this task.
policy:
  allow_implicit_invocation: false
