interface:
  display_name: Data Schema
  short_description: Use when doing data schema work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure path be
  default_prompt: Use the data-schema skill for this task.
policy:
  allow_implicit_invocation: false
