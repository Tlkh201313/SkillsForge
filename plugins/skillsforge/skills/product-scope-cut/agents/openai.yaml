interface:
  display_name: Product Scope Cut
  short_description: Use when you need product scope cut in a SkillsForge product workflow.
  default_prompt: Use the product-scope-cut skill for this task.
policy:
  allow_implicit_invocation: true
