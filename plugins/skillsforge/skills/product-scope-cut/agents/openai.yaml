interface:
  display_name: Product Scope Cut
  short_description: Use when doing product scope cut work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-scope-cut skill for this task.
policy:
  allow_implicit_invocation: false
