interface:
  display_name: Data Analytics Plan
  short_description: Use when doing data analytics plan work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure
  default_prompt: Use the data-analytics-plan skill for this task.
policy:
  allow_implicit_invocation: false
