interface:
  display_name: Data Analytics Plan
  short_description: Use when doing data analytics plan work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-analytics-plan skill for this task.
policy:
  allow_implicit_invocation: false
