interface:
  display_name: Fullstack Api Contract
  short_description: Use when doing fullstack api contract work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollb
  default_prompt: Use the fullstack-api-contract skill for this task.
policy:
  allow_implicit_invocation: false
