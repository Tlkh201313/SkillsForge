interface:
  display_name: Role Ceo Review
  short_description: Use when doing role ceo review work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-ceo-review skill for this task.
policy:
  allow_implicit_invocation: false
