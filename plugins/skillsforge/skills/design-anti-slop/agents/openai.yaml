interface:
  display_name: Design Anti Slop
  short_description: Use when doing design anti slop work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-anti-slop skill for this task.
policy:
  allow_implicit_invocation: false
