interface:
  display_name: Content Docs Voice
  short_description: Use when doing content docs voice work and you need bounded content-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the content-docs-voice skill for this task.
policy:
  allow_implicit_invocation: false
