interface:
  display_name: Cloud Observability
  short_description: Use when doing cloud observability work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and ru
  default_prompt: Use the cloud-observability skill for this task.
policy:
  allow_implicit_invocation: false
