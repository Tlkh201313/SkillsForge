interface:
  display_name: Mobile Permissions
  short_description: Use when doing mobile permissions work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-permissions skill for this task.
policy:
  allow_implicit_invocation: false
