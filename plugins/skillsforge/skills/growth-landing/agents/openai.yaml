interface:
  display_name: Growth Landing
  short_description: Use when doing growth landing work and you need bounded growth-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the growth-landing skill for this task.
policy:
  allow_implicit_invocation: false
