interface:
  display_name: Design Crud Flow
  short_description: Use when doing design crud flow work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility chec
  default_prompt: Use the design-crud-flow skill for this task.
policy:
  allow_implicit_invocation: false
