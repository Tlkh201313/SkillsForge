interface:
  display_name: Test Unit
  short_description: Use when doing test unit work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-unit skill for this task.
policy:
  allow_implicit_invocation: false
