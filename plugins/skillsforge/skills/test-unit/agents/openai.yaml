interface:
  display_name: Test Unit
  short_description: Use when doing test unit work for testing-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the test-unit skill for this task.
policy:
  allow_implicit_invocation: false
