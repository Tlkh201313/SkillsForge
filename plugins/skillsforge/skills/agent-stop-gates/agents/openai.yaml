interface:
  display_name: Agent Stop Gates
  short_description: Use when you need agent stop gates in a SkillsForge agentic workflow.
  default_prompt: Use the agent-stop-gates skill for this task.
policy:
  allow_implicit_invocation: false
