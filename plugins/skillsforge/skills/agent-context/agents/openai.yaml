interface:
  display_name: Agent Context
  short_description: Use when doing agent context work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-context skill for this task.
policy:
  allow_implicit_invocation: false
