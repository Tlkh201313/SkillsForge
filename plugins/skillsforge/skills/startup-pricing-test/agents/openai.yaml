interface:
  display_name: Startup Pricing Test
  short_description: Use when doing startup pricing test work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and p
  default_prompt: Use the startup-pricing-test skill for this task.
policy:
  allow_implicit_invocation: false
