interface:
  display_name: Role Founder
  short_description: Use when doing role founder work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-founder skill for this task.
policy:
  allow_implicit_invocation: false
