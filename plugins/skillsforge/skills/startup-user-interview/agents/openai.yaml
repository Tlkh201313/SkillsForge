interface:
  display_name: Startup User Interview
  short_description: Use when doing startup user interview work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and
  default_prompt: Use the startup-user-interview skill for this task.
policy:
  allow_implicit_invocation: false
