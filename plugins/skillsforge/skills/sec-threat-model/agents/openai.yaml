interface:
  display_name: Sec Threat Model
  short_description: Use when doing sec threat model work and you need bounded security-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the sec-threat-model skill for this task.
policy:
  allow_implicit_invocation: false
