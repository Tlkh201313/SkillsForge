interface:
  display_name: Fw Vue
  short_description: Use when doing fw vue work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-vue skill for this task.
policy:
  allow_implicit_invocation: false
