interface:
  display_name: Product Pricing Signal
  short_description: Use when doing product pricing signal work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and
  default_prompt: Use the product-pricing-signal skill for this task.
policy:
  allow_implicit_invocation: false
