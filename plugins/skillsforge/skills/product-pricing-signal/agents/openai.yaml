interface:
  display_name: Product Pricing Signal
  short_description: Use when doing product pricing signal work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-pricing-signal skill for this task.
policy:
  allow_implicit_invocation: false
