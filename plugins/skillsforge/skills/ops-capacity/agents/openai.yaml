interface:
  display_name: Ops Capacity
  short_description: Use when doing ops capacity work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-capacity skill for this task.
policy:
  allow_implicit_invocation: false
