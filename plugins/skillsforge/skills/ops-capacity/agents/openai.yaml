interface:
  display_name: Ops Capacity
  short_description: Use when doing ops capacity work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook n
  default_prompt: Use the ops-capacity skill for this task.
policy:
  allow_implicit_invocation: false
