interface:
  display_name: Build Zcode Pack
  short_description: Use when doing build zcode pack work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation path befo
  default_prompt: Use the build-zcode-pack skill for this task.
policy:
  allow_implicit_invocation: false
