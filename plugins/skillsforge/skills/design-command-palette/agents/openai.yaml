interface:
  display_name: Design Command Palette
  short_description: Use when doing design command palette work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-command-palette skill for this task.
policy:
  allow_implicit_invocation: false
