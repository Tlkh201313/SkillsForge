interface:
  display_name: Design Command Palette
  short_description: Use when doing design command palette work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibilit
  default_prompt: Use the design-command-palette skill for this task.
policy:
  allow_implicit_invocation: false
