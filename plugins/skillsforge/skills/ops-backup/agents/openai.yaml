interface:
  display_name: Ops Backup
  short_description: Use when doing ops backup work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-backup skill for this task.
policy:
  allow_implicit_invocation: false
