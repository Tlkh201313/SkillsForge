interface:
  display_name: Eng Versioning
  short_description: Use when doing eng versioning work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-versioning skill for this task.
policy:
  allow_implicit_invocation: false
