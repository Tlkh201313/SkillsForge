interface:
  display_name: Ops Cost
  short_description: Use when doing ops cost work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-cost skill for this task.
policy:
  allow_implicit_invocation: false
