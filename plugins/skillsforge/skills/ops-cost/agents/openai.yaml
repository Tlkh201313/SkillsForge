interface:
  display_name: Ops Cost
  short_description: Use when you need ops cost in a SkillsForge ops workflow.
  default_prompt: Use the ops-cost skill for this task.
policy:
  allow_implicit_invocation: false
