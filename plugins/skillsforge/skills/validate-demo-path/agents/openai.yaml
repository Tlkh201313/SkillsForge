interface:
  display_name: Validate Demo Path
  short_description: Use when doing validate demo path work for real-task proof, claim audit, package verification, and launch readiness and you need claim list, evidence source, command result, pass/fail gate, and unreso
  default_prompt: Use the validate-demo-path skill for this task.
policy:
  allow_implicit_invocation: false
