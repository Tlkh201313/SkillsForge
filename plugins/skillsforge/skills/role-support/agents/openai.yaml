interface:
  display_name: Role Support
  short_description: Use when doing role support work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-support skill for this task.
policy:
  allow_implicit_invocation: false
