interface:
  display_name: Role Support
  short_description: Use when you need role support in a SkillsForge roles workflow.
  default_prompt: Use the role-support skill for this task.
policy:
  allow_implicit_invocation: false
