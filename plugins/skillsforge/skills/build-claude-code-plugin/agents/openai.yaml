interface:
  display_name: Build Claude Code Plugin
  short_description: Use when doing build claude code plugin work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation p
  default_prompt: Use the build-claude-code-plugin skill for this task.
policy:
  allow_implicit_invocation: false
