interface:
  display_name: Design Visual Hierarchy
  short_description: Use when doing design visual hierarchy work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibili
  default_prompt: Use the design-visual-hierarchy skill for this task.
policy:
  allow_implicit_invocation: false
