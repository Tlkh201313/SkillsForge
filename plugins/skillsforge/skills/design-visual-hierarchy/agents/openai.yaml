interface:
  display_name: Design Visual Hierarchy
  short_description: Use when doing design visual hierarchy work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-visual-hierarchy skill for this task.
policy:
  allow_implicit_invocation: false
