interface:
  display_name: Sec Disclosure
  short_description: Use when doing sec disclosure work for permission, privacy, boundary, dependency, and threat-model work and you need asset, actor, boundary, failure path, mitigation, and verification evidence before
  default_prompt: Use the sec-disclosure skill for this task.
policy:
  allow_implicit_invocation: false
