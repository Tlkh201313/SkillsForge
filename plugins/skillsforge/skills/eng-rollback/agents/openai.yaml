interface:
  display_name: Eng Rollback
  short_description: Use when doing eng rollback work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-rollback skill for this task.
policy:
  allow_implicit_invocation: false
