interface:
  display_name: Sec Incident
  short_description: Use when doing sec incident work for permission, privacy, boundary, dependency, and threat-model work and you need asset, actor, boundary, failure path, mitigation, and verification evidence before cl
  default_prompt: Use the sec-incident skill for this task.
policy:
  allow_implicit_invocation: false
