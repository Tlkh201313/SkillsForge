interface:
  display_name: Lang Graphql
  short_description: Use when doing lang graphql work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-graphql skill for this task.
policy:
  allow_implicit_invocation: false
