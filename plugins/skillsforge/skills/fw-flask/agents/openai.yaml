interface:
  display_name: Fw Flask
  short_description: Use when doing fw flask work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-flask skill for this task.
policy:
  allow_implicit_invocation: false
