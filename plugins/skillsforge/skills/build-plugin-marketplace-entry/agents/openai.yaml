interface:
  display_name: Build Plugin Marketplace Entry
  short_description: Use when doing build plugin marketplace entry work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and valida
  default_prompt: Use the build-plugin-marketplace-entry skill for this task.
policy:
  allow_implicit_invocation: false
