interface:
  display_name: Eng Dead Code
  short_description: Use when doing eng dead code work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollback note,
  default_prompt: Use the eng-dead-code skill for this task.
policy:
  allow_implicit_invocation: false
