interface:
  display_name: Eng Dead Code
  short_description: Use when doing eng dead code work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-dead-code skill for this task.
policy:
  allow_implicit_invocation: false
