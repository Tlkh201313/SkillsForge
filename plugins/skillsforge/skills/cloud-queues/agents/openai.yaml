interface:
  display_name: Cloud Queues
  short_description: Use when doing cloud queues work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and runbook n
  default_prompt: Use the cloud-queues skill for this task.
policy:
  allow_implicit_invocation: false
