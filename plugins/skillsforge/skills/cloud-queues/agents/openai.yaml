interface:
  display_name: Cloud Queues
  short_description: Use when doing cloud queues work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-queues skill for this task.
policy:
  allow_implicit_invocation: false
