interface:
  display_name: Eng Ci Pipeline
  short_description: Use when doing eng ci pipeline work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-ci-pipeline skill for this task.
policy:
  allow_implicit_invocation: false
