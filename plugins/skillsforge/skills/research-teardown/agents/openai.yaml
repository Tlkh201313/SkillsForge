interface:
  display_name: Research Teardown
  short_description: Use when doing research teardown work and you need bounded research-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the research-teardown skill for this task.
policy:
  allow_implicit_invocation: false
