interface:
  display_name: Test Security
  short_description: Use when doing test security work for testing-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the test-security skill for this task.
policy:
  allow_implicit_invocation: false
