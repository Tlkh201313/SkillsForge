interface:
  display_name: Fw Spring
  short_description: Use when doing fw spring work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-spring skill for this task.
policy:
  allow_implicit_invocation: false
