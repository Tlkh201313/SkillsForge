interface:
  display_name: Sec Session
  short_description: Use when doing sec session work for permission, privacy, boundary, dependency, and threat-model work and you need asset, actor, boundary, failure path, mitigation, and verification evidence before cla
  default_prompt: Use the sec-session skill for this task.
policy:
  allow_implicit_invocation: false
