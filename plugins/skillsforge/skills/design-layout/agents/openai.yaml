interface:
  display_name: Design Layout
  short_description: Use when doing design layout work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility check,
  default_prompt: Use the design-layout skill for this task.
policy:
  allow_implicit_invocation: false
