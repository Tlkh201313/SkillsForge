interface:
  display_name: Design Layout
  short_description: Use when doing design layout work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-layout skill for this task.
policy:
  allow_implicit_invocation: false
