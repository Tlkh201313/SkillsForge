interface:
  display_name: Product Rice
  short_description: Use when doing product rice work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and proof nee
  default_prompt: Use the product-rice skill for this task.
policy:
  allow_implicit_invocation: false
