interface:
  display_name: Design Mobile Touch
  short_description: Use when doing design mobile touch work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-mobile-touch skill for this task.
policy:
  allow_implicit_invocation: false
