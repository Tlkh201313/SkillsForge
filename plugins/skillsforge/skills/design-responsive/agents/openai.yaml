interface:
  display_name: Design Responsive
  short_description: Use when doing design responsive work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-responsive skill for this task.
policy:
  allow_implicit_invocation: false
