interface:
  display_name: Build Codex Plugin
  short_description: Use when doing build codex plugin work for AI CLI, plugin, MCP, skill, or full-stack builder work and you need host matrix, interface contract, scaffold boundary, smoke command, and validation path be
  default_prompt: Use the build-codex-plugin skill for this task.
policy:
  allow_implicit_invocation: false
