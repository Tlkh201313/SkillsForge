interface:
  display_name: Eng Module Boundaries
  short_description: Use when doing eng module boundaries work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollba
  default_prompt: Use the eng-module-boundaries skill for this task.
policy:
  allow_implicit_invocation: false
