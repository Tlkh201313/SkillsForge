interface:
  display_name: Eng Module Boundaries
  short_description: Use when doing eng module boundaries work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-module-boundaries skill for this task.
policy:
  allow_implicit_invocation: false
