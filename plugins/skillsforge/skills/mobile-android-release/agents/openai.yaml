interface:
  display_name: Mobile Android Release
  short_description: Use when doing mobile android release work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-android-release skill for this task.
policy:
  allow_implicit_invocation: false
