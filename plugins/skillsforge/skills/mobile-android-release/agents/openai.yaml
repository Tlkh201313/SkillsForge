interface:
  display_name: Mobile Android Release
  short_description: Use when doing mobile android release work for mobile-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the mobile-android-release skill for this task.
policy:
  allow_implicit_invocation: false
