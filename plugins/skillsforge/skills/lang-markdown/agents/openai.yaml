interface:
  display_name: Lang Markdown
  short_description: Use when doing lang markdown work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-markdown skill for this task.
policy:
  allow_implicit_invocation: false
