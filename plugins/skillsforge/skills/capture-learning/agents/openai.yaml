interface:
  display_name: Capture Learning
  short_description: Use when doing capture learning work for lifecycle-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the capture-learning skill for this task.
policy:
  allow_implicit_invocation: false
