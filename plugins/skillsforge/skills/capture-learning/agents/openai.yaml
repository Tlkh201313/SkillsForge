interface:
  display_name: Capture Learning
  short_description: Use when doing capture learning work and you need bounded lifecycle-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the capture-learning skill for this task.
policy:
  allow_implicit_invocation: false
