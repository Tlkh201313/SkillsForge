interface:
  display_name: Startup Waitlist Loop
  short_description: Use when doing startup waitlist loop work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and
  default_prompt: Use the startup-waitlist-loop skill for this task.
policy:
  allow_implicit_invocation: false
