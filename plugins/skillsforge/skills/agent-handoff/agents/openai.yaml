interface:
  display_name: Agent Handoff
  short_description: Use when doing agent handoff work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-handoff skill for this task.
policy:
  allow_implicit_invocation: false
