interface:
  display_name: Agent Handoff
  short_description: Use when you need agent handoff in a SkillsForge agentic workflow.
  default_prompt: Use the agent-handoff skill for this task.
policy:
  allow_implicit_invocation: true
