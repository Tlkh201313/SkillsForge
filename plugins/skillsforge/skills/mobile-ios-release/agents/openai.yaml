interface:
  display_name: Mobile Ios Release
  short_description: Use when doing mobile ios release work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-ios-release skill for this task.
policy:
  allow_implicit_invocation: false
