interface:
  display_name: Mobile Ios Release
  short_description: Use when doing mobile ios release work for mobile-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the mobile-ios-release skill for this task.
policy:
  allow_implicit_invocation: false
