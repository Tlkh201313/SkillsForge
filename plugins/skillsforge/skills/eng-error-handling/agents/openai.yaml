interface:
  display_name: Eng Error Handling
  short_description: Use when doing eng error handling work and you need bounded eng-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the eng-error-handling skill for this task.
policy:
  allow_implicit_invocation: false
