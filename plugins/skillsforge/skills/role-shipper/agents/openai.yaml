interface:
  display_name: Role Shipper
  short_description: Use when doing role shipper work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-shipper skill for this task.
policy:
  allow_implicit_invocation: false
