interface:
  display_name: Lang Ruby
  short_description: Use when doing lang ruby work for lang-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the lang-ruby skill for this task.
policy:
  allow_implicit_invocation: false
