interface:
  display_name: Lang Ruby
  short_description: Use when doing lang ruby work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-ruby skill for this task.
policy:
  allow_implicit_invocation: false
