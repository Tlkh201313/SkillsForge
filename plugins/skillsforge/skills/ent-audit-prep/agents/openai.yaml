interface:
  display_name: Ent Audit Prep
  short_description: Use when doing ent audit prep work and you need bounded enterprise-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ent-audit-prep skill for this task.
policy:
  allow_implicit_invocation: false
