interface:
  display_name: Design Motion
  short_description: Use when doing design motion work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-motion skill for this task.
policy:
  allow_implicit_invocation: false
