interface:
  display_name: Agent Prompt Budget
  short_description: Use when doing agent prompt budget work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-prompt-budget skill for this task.
policy:
  allow_implicit_invocation: false
