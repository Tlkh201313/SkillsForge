interface:
  display_name: Agent Prompt Budget
  short_description: Use when doing agent prompt budget work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay path,
  default_prompt: Use the agent-prompt-budget skill for this task.
policy:
  allow_implicit_invocation: false
