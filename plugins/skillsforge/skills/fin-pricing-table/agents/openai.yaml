interface:
  display_name: Fin Pricing Table
  short_description: Use when doing fin pricing table work for finance-lite-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fin-pricing-table skill for this task.
policy:
  allow_implicit_invocation: false
