interface:
  display_name: Fullstack Deploy Plan
  short_description: Use when doing fullstack deploy plan work for repo-grounded full-stack architecture, implementation planning, and engineering gates and you need system map, contract, affected files, test plan, rollba
  default_prompt: Use the fullstack-deploy-plan skill for this task.
policy:
  allow_implicit_invocation: false
