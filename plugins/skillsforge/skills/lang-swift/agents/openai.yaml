interface:
  display_name: Lang Swift
  short_description: Use when doing lang swift work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-swift skill for this task.
policy:
  allow_implicit_invocation: false
