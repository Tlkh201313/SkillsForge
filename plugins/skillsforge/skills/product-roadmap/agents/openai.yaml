interface:
  display_name: Product Roadmap
  short_description: Use when doing product roadmap work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and proof
  default_prompt: Use the product-roadmap skill for this task.
policy:
  allow_implicit_invocation: false
