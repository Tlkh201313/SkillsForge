interface:
  display_name: Ops Slos
  short_description: Use when doing ops slos work and you need bounded ops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the ops-slos skill for this task.
policy:
  allow_implicit_invocation: false
