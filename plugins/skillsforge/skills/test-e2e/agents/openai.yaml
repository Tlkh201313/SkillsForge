interface:
  display_name: Test E2e
  short_description: Use when doing test e2e work for testing-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the test-e2e skill for this task.
policy:
  allow_implicit_invocation: false
