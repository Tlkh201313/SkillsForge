interface:
  display_name: Test Snapshot
  short_description: Use when doing test snapshot work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-snapshot skill for this task.
policy:
  allow_implicit_invocation: false
