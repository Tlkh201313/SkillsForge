interface:
  display_name: Role Growth Lead
  short_description: Use when doing role growth lead work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-growth-lead skill for this task.
policy:
  allow_implicit_invocation: false
