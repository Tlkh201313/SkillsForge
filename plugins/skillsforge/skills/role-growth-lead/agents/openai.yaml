interface:
  display_name: Role Growth Lead
  short_description: Use when doing role growth lead work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-growth-lead skill for this task.
policy:
  allow_implicit_invocation: false
