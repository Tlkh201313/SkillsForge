interface:
  display_name: Docs Adr
  short_description: Use when doing docs adr work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-adr skill for this task.
policy:
  allow_implicit_invocation: false
