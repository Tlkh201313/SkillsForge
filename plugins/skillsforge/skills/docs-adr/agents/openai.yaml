interface:
  display_name: Docs Adr
  short_description: Use when doing docs adr work for agent-facing docs, launch media, demo proof, and content production and you need audience, source facts, outline, asset path, claim proof, and publishing check before
  default_prompt: Use the docs-adr skill for this task.
policy:
  allow_implicit_invocation: false
