interface:
  display_name: Lang Kotlin
  short_description: Use when doing lang kotlin work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-kotlin skill for this task.
policy:
  allow_implicit_invocation: false
