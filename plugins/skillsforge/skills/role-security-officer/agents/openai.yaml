interface:
  display_name: Role Security Officer
  short_description: Use when doing role security officer work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-security-officer skill for this task.
policy:
  allow_implicit_invocation: false
