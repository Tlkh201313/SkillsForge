interface:
  display_name: Role Security Officer
  short_description: Use when doing role security officer work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-security-officer skill for this task.
policy:
  allow_implicit_invocation: false
