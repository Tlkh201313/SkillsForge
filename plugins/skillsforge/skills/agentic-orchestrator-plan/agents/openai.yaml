interface:
  display_name: Agentic Orchestrator Plan
  short_description: Use when doing agentic orchestrator plan work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay
  default_prompt: Use the agentic-orchestrator-plan skill for this task.
policy:
  allow_implicit_invocation: false
