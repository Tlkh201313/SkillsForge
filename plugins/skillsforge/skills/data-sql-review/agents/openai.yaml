interface:
  display_name: Data Sql Review
  short_description: Use when doing data sql review work for analytics, events, schema, metrics, dashboard, and data-quality work and you need event/schema definition, owner, validation rule, sample query, and failure pat
  default_prompt: Use the data-sql-review skill for this task.
policy:
  allow_implicit_invocation: false
