interface:
  display_name: Data Sql Review
  short_description: Use when doing data sql review work and you need bounded data-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the data-sql-review skill for this task.
policy:
  allow_implicit_invocation: false
