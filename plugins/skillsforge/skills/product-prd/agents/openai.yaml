interface:
  display_name: Product Prd
  short_description: Use when doing product prd work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and proof need
  default_prompt: Use the product-prd skill for this task.
policy:
  allow_implicit_invocation: false
