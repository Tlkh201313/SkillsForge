interface:
  display_name: Product Prd
  short_description: Use when doing product prd work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-prd skill for this task.
policy:
  allow_implicit_invocation: false
