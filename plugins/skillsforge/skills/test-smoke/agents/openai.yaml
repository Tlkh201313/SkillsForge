interface:
  display_name: Test Smoke
  short_description: Use when doing test smoke work and you need bounded testing-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the test-smoke skill for this task.
policy:
  allow_implicit_invocation: false
