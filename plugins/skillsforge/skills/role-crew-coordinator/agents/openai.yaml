interface:
  display_name: Role Crew Coordinator
  short_description: Use when doing role crew coordinator work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-crew-coordinator skill for this task.
policy:
  allow_implicit_invocation: false
