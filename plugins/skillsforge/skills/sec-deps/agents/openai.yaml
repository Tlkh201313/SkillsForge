interface:
  display_name: Sec Deps
  short_description: Use when doing sec deps work and you need bounded security-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the sec-deps skill for this task.
policy:
  allow_implicit_invocation: false
