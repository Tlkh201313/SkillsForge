interface:
  display_name: Build Fullstack Admin
  short_description: Use when doing build fullstack admin work for full-stack SaaS, CRUD, auth, admin, MVP, and launch-proof builder work and you need repo map, data model, auth boundary, UI flow, deploy path, test plan,
  default_prompt: Use the build-fullstack-admin skill for this task.
policy:
  allow_implicit_invocation: false
