interface:
  display_name: Fw Laravel
  short_description: Use when doing fw laravel work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-laravel skill for this task.
policy:
  allow_implicit_invocation: false
