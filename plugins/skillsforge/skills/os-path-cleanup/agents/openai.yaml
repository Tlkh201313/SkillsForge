interface:
  display_name: Os Path Cleanup
  short_description: Use when you need os path cleanup in a SkillsForge os workflow.
  default_prompt: Use the os-path-cleanup skill for this task.
policy:
  allow_implicit_invocation: false
