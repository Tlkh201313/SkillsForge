interface:
  display_name: Os Path Cleanup
  short_description: Use when doing os path cleanup work and you need bounded os-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the os-path-cleanup skill for this task.
policy:
  allow_implicit_invocation: false
