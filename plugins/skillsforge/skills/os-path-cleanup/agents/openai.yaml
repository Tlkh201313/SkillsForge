interface:
  display_name: Os Path Cleanup
  short_description: Use when doing os path cleanup work for os-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the os-path-cleanup skill for this task.
policy:
  allow_implicit_invocation: false
