interface:
  display_name: Agent Parallel
  short_description: Use when you need agent parallel in a SkillsForge agentic workflow.
  default_prompt: Use the agent-parallel skill for this task.
policy:
  allow_implicit_invocation: false
