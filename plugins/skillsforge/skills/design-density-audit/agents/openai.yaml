interface:
  display_name: Design Density Audit
  short_description: Use when doing design density audit work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility
  default_prompt: Use the design-density-audit skill for this task.
policy:
  allow_implicit_invocation: false
