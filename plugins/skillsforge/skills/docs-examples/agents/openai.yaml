interface:
  display_name: Docs Examples
  short_description: Use when doing docs examples work and you need bounded docs-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the docs-examples skill for this task.
policy:
  allow_implicit_invocation: false
