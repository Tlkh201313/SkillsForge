interface:
  display_name: Cloud Storage
  short_description: Use when doing cloud storage work and you need bounded cloud-devops-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the cloud-storage skill for this task.
policy:
  allow_implicit_invocation: false
