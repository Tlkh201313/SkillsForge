interface:
  display_name: Agent Memory Lite
  short_description: Use when doing agent memory lite work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-memory-lite skill for this task.
policy:
  allow_implicit_invocation: false
