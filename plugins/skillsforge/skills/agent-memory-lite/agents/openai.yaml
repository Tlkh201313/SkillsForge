interface:
  display_name: Agent Memory Lite
  short_description: Use when doing agent memory lite work for agent workflow planning, replay, routing, tool policy, and token-budget work and you need agent role, tool boundary, context budget, stop gate, replay path, a
  default_prompt: Use the agent-memory-lite skill for this task.
policy:
  allow_implicit_invocation: false
