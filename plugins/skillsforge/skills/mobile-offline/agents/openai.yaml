interface:
  display_name: Mobile Offline
  short_description: Use when doing mobile offline work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-offline skill for this task.
policy:
  allow_implicit_invocation: false
