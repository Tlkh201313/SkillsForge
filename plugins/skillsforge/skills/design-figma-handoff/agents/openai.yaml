interface:
  display_name: Design Figma Handoff
  short_description: Use when doing design figma handoff work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-figma-handoff skill for this task.
policy:
  allow_implicit_invocation: false
