interface:
  display_name: Design Data Dense Filtering
  short_description: Use when doing design data dense filtering work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessi
  default_prompt: Use the design-data-dense-filtering skill for this task.
policy:
  allow_implicit_invocation: false
