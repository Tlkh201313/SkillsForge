interface:
  display_name: Role Data Analyst
  short_description: Use when doing role data analyst work for roles-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the role-data-analyst skill for this task.
policy:
  allow_implicit_invocation: false
