interface:
  display_name: Role Data Analyst
  short_description: Use when doing role data analyst work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-data-analyst skill for this task.
policy:
  allow_implicit_invocation: false
