interface:
  display_name: Design Forms
  short_description: Use when doing design forms work and you need bounded design-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the design-forms skill for this task.
policy:
  allow_implicit_invocation: false
