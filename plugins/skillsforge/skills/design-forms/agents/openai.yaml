interface:
  display_name: Design Forms
  short_description: Use when doing design forms work for dense product UI, design system, motion, accessibility, and visual QA work and you need layout decision, component states, responsive proof, accessibility check, a
  default_prompt: Use the design-forms skill for this task.
policy:
  allow_implicit_invocation: false
