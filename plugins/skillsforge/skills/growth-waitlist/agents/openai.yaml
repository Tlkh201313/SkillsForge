interface:
  display_name: Growth Waitlist
  short_description: Use when doing growth waitlist work and you need bounded growth-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the growth-waitlist skill for this task.
policy:
  allow_implicit_invocation: false
