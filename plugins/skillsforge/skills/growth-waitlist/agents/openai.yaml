interface:
  display_name: Growth Waitlist
  short_description: Use when doing growth waitlist work for launch, SEO, referral, email, proof, and growth loop work and you need audience, channel, offer, experiment, metric, creative asset, and verification step befor
  default_prompt: Use the growth-waitlist skill for this task.
policy:
  allow_implicit_invocation: false
