interface:
  display_name: Product Feedback
  short_description: Use when doing product feedback work and you need bounded product-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the product-feedback skill for this task.
policy:
  allow_implicit_invocation: false
