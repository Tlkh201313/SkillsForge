interface:
  display_name: Product Feedback
  short_description: Use when doing product feedback work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and proof
  default_prompt: Use the product-feedback skill for this task.
policy:
  allow_implicit_invocation: false
