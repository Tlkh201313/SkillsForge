interface:
  display_name: Ops Incident Comms
  short_description: Use when doing ops incident comms work for environment, CI, deployment, reliability, and operational readiness work and you need environment map, failing signal, command path, rollback option, and run
  default_prompt: Use the ops-incident-comms skill for this task.
policy:
  allow_implicit_invocation: false
