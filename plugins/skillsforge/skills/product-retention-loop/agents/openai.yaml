interface:
  display_name: Product Retention Loop
  short_description: Use when doing product retention loop work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and
  default_prompt: Use the product-retention-loop skill for this task.
policy:
  allow_implicit_invocation: false
