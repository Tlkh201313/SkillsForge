interface:
  display_name: Product Paywall Fit
  short_description: Use when doing product paywall fit work for startup, MVP, onboarding, activation, pricing, and product strategy work and you need user segment, problem statement, scope cut, experiment, metric, and pr
  default_prompt: Use the product-paywall-fit skill for this task.
policy:
  allow_implicit_invocation: false
