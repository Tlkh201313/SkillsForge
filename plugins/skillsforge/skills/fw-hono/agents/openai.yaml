interface:
  display_name: Fw Hono
  short_description: Use when doing fw hono work and you need bounded framework-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the fw-hono skill for this task.
policy:
  allow_implicit_invocation: false
