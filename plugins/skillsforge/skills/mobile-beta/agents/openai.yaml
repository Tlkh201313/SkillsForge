interface:
  display_name: Mobile Beta
  short_description: Use when doing mobile beta work and you need bounded mobile-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the mobile-beta skill for this task.
policy:
  allow_implicit_invocation: false
