interface:
  display_name: Mobile Beta
  short_description: Use when doing mobile beta work for mobile-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the mobile-beta skill for this task.
policy:
  allow_implicit_invocation: false
