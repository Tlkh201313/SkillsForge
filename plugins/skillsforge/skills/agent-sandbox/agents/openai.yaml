interface:
  display_name: Agent Sandbox
  short_description: Use when doing agent sandbox work and you need bounded agentic-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the agent-sandbox skill for this task.
policy:
  allow_implicit_invocation: false
