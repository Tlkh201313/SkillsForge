interface:
  display_name: Agent Sandbox
  short_description: Use when you need agent sandbox in a SkillsForge agentic workflow.
  default_prompt: Use the agent-sandbox skill for this task.
policy:
  allow_implicit_invocation: false
