interface:
  display_name: Role Researcher
  short_description: Use when doing role researcher work and you need bounded roles-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the role-researcher skill for this task.
policy:
  allow_implicit_invocation: false
