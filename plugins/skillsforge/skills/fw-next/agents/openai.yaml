interface:
  display_name: Fw Next
  short_description: Use when doing fw next work for framework-pack work and you need bounded artifact, evidence, risk note, and verification command before claiming progress.
  default_prompt: Use the fw-next skill for this task.
policy:
  allow_implicit_invocation: false
