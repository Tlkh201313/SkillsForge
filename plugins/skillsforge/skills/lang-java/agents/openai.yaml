interface:
  display_name: Lang Java
  short_description: Use when doing lang java work and you need bounded lang-pack steps, stop conditions, and a verification check before shipping.
  default_prompt: Use the lang-java skill for this task.
policy:
  allow_implicit_invocation: false
