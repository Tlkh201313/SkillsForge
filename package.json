{
  "name": "skillsforge",
  "version": "0.2.0",
  "description": "Validate, route, policy-scan, and compile agent skills from one canonical source.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate": "node scripts/validate-skill.mjs --all",
    "test": "node scripts/test.mjs",
    "route": "node scripts/route.mjs",
    "sync": "node scripts/sync.mjs",
    "build": "node scripts/build.mjs",
    "eval": "node scripts/eval.mjs"
  },
  "license": "MIT"
}
