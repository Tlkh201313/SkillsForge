# Validator Agent

Use this agent definition for adversarial validation once Phase 3 activates gated orchestration.

Phase 0 keeps the file as part of the required product layout. Current validation runs through `scripts/validate-skill.mjs` and `npm test`.
